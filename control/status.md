# trading-lab · status
updated: 2026-07-10T16:21:48Z
phase: ORDER 007 DONE — promotion-significance bar coded + AAPL-donchian re-graded (DEMOTED to RULE-PASS / candidate; no candidate currently holds a finding label). Lane otherwise parked green. NEXT: ORDER 008 — P5 one-shot holdout evaluation, to be run by a FRESH dedicated session with docs/p5-holdout-protocol.md binding (protocol §7; this session is not that session and only acks 008). The holdout stayed SEALED through ORDER 007: zero bars loaded, zero backtests run, no holdout_unlocked marker anywhere.
health: green — 147 tests passed locally (133 prior + 14 new promotion tests) + `python3 bootstrap.py check --strict --require-session-log` exit 0 on the ORDER 007 branch; PR #36 merged on green via REST squash (required checks tests + substrate-gate verified before merge; auto-merge arm failed pending-side with the known "unstable status" wall — not retried, fallback path used).
last PR: #36 — ORDER 007: promotion-significance bar + AAPL-donchian re-grade (branch order-007-significance-bar, https://github.com/menno420/trading-strategy/pull/36)
routine state: ARMED AND RECURRING — trigger trig_01Mvn5xRmqGmZJNRHgjqyLpN, cron "0 */4 * * *" (unchanged from close-out; confirmed fires through 12:00Z 2026-07-10).
blockers: none. ORDER 007's gate on holdout use is now cleared; ORDER 008 (P5 unlock, GRANTED via Q-0262.2) is executable by a dedicated session per the binding protocol.
orders: acked=001–008, done=001–007. ORDER 007 (2026-07-10T12:47:35Z) DONE this session: significance rule in code (trading_lab.promotion — minimum t-stat on the Sharpe delta vs B&H, Lo 2002 SE, Bonferroni-adjusted for variants tried; the founding plan's deflated-Sharpe preference as an explicit equivalent, justified in the module and ledger entry) + pytest coverage; AAPL-donchian 15/5 re-graded honestly from the ledgered P2 numbers (edge +0.0788 Sharpe, SE 0.1855 at N=7,331, t = 0.42 < 1.64 at the most lenient K=1; K=41/177 raise the bar to 3.03/3.45) → DEMOTED to candidate; first-class ledger entry docs/p2-regrade-aapl-donchian.md; labels updated in docs/p2-validation-results.md, docs/final-report.md, docs/succession/QUEUE.md, docs/current-state.md, docs/p4-transfer-results.md. docs/p5-holdout-protocol.md NOT edited (binding, pre-registered); its §1 prose label of AAPL-donchian as "the sole P2 PROMOTED-TO-FINDING" is superseded by the re-grade entry — the subject remains PRIMARY, params/windows/verdict rules unchanged. **ORDER 008 (2026-07-10T15:33Z) ACKED, NOT EXECUTED here — NEXT:** P5 one-shot holdout evaluation, dedicated fresh session, docs/p5-holdout-protocol.md binding, sequencing satisfied now that 007 is done. Inbox re-read at origin/main HEAD (fd5e9fe) 2026-07-10T16:21:48Z immediately before this write: no orders newer than 008.
⚑ needs-owner (six-field form — what / why / exact click / where / blocking? / fallback):
- (b) ENV SETUP SCRIPT / fresh-environment sessions die silently at provision without it (setup runs at cwd=/home/user with repos as subdirectories) / paste the contents of environments/setup-universal.sh into the environment's setup-script field and save / Claude environment config for this project / not blocking (current pinned env works) / keep spawning into the pinned env and treat any child with no heartbeat within 10 min as dead → respawn.
- (c) ALLOW AUTO-MERGE TOGGLE / auto-merge arm fails pending-side with "unstable status" while the repo toggle is off, so every merge needs an agent to poll-and-merge (reconfirmed on PR #36) / tick "Allow auto-merge" / GitHub → menno420/trading-strategy → Settings → General → Pull Requests / not blocking / continue REST squash-merge on green (the path every merge to date has used).
- (d) ARCHIVE DEAD GEN-1 SESSION / the gen-1 "ORDER 001 successor" session died at provision, emits no failure event, and still lists as active — misleading / open the project's session list, locate "ORDER 001 successor", archive it / claude.ai session list for this project / not blocking / ignore it — it consumes nothing.
next-update-by: 2026-07-11T00:22:00Z
notes: the former ⚑ (a) P5 unlock ask is dropped — granted by ORDER 008 (Q-0262.2). Claim claims/order-007-significance-bar.md created and released within PR #36; claims/ holds only its README after merge. Forward-only git preserved; control/inbox.md read-only, untouched; no model identifiers in repo per session policy. For the ORDER 008 session: the PRIMARY subject's label is now candidate (RULE-PASS, demoted 2026-07-10) — see docs/p2-regrade-aapl-donchian.md; a holdout CONFIRMED would confirm a candidate, not validate a pre-existing finding.
