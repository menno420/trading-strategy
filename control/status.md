# trading-lab · status
updated: 2026-07-10T22:03:07Z
phase: PAPER LANE OPERATIONAL — foundation complete on main. The standing mission after ORDER 008 (holdout SPENT, program complete, final report on main as ffdd6f6) is the forward paper lane, and its full foundation landed this session in four merged PRs: #40 design docs (d0fc23b), #41 pre-registered protocol docs/paper-lane-protocol.md — BINDING, committed before any trade outcome is observable (8e512c1), #42 loader rail + ledger (35cc0c1), #43 grading job (0f76e2e). Next work: weekly grading passes per protocol §6–§7 + the research backlog. No real money, no accounts, no orders — ever.
paper lane: opens 2026-07-11 (PAPER_LANE_START, pinned in config + test). State: strategy FLAT, warm-up pending — ledger record paper-0001 WATCH in experiments/paper/ledger.md; zero lane bars exist yet, the donchian(15,5) channels build only from bars ≥ 2026-07-11 (§9 A1), so the first evaluable entry signal comes only after 15 lane trading bars (~early Aug 2026), entry thereafter t+1-open on channel breakout per the frozen rule. Weekly grading cadence per protocol §6–§7 via scripts/grade_paper.py (trading_lab.paper.grade_ledger): BEAT/MISS/FLAT vs cycle-window B&H, idempotent, WATCH/ENTRY rows never mutated. Ambiguities A1–A6 resolved against the strategy in the protocol, plus the grading job's extensions (late/missing commit timestamp on EITHER leg = MISS; 13:30 UTC open bound, the stricter year-round; exact ties = MISS; malformed ledger grades nothing). Market data for the lane flows ONLY through trading_lab.data.load_paper_ohlcv (bars ≥ PAPER_LANE_START only, no unlock parameter, holdout rail untouched — guard-tested).
health: green — full suite 165 passed on main after #43 (147 → 154 with #42, → 165 with #43); both required checks (pytest + substrate-gate) green on PRs #42 and #43 at merge; bootstrap check --strict green.
kit: substrate-kit v1.7.0 — upgraded from v1.1.0 via PR #38 (commit 0e713b9).
last PR: #43 — paper lane: grading job (closed-window BEAT/MISS/FLAT vs cycle-window B&H, idempotent) — MERGED as 0f76e2e (https://github.com/menno420/trading-strategy/pull/43). Also landed this session: #40 design docs (d0fc23b), #41 paper-lane protocol (8e512c1), #42 loader rail load_paper_ohlcv ≥ PAPER_LANE_START=2026-07-11 + experiments/paper/ledger.md with record paper-0001 WATCH (35cc0c1).
routine state: CUTOVER COMPLETE — created + verified trigger trig_01YBaVeKAW2fSD83S9F37s2d "trading-strategy failsafe wake" cron "0 */2 * * *" bound to coordinator session session_01NwvvbgUVSdQvY8eYwtuEoo (created_via meta_mcp, 2026-07-10T21:03:05Z), THEN deleted predecessor trig_01Mvn5xRmqGmZJNRHgjqyLpN (result: "deleted trigger trig_01Mvn5xRmqGmZJNRHgjqyLpN"); send_later continuation armed (trig_01QWpCYS8wBdX3Pa1sDq6Gk1, fires 2026-07-10T21:20:00Z).
holdout: **SPENT** — and UNTOUCHED this session. The one-shot was consumed 2026-07-10 (run stamps 2026-07-10T1647Z); all 13 ledger rows carry `holdout_unlocked=true`. Per protocol §6 there will be NO further tuning, NO re-runs, NO new windows or variants — ever. The paper-lane work added no holdout access: the new rail cannot serve a bar < PAPER_LANE_START, takes no unlock parameter, and every pre-existing holdout guard test still passes; the grading job has zero holdout surface (source-guard tested).
verdict: primary donchian×AAPL×daily CONFIRMED by the mechanical §5 rule (Sharpe 0.759 > B&H 0.740) but t = 0.02 → remains a RULE-PASS candidate under the ORDER 007 significance rule, NOT a finding; secondaries 2/12 HOLDOUT-BEAT (S5, S6) — chance-level vs the pre-registered null; 0 of 13 clears the significance bar even at the most lenient K=1. No NOT-EVALUABLE. Authoritative detail: docs/final-report.md §Holdout (on main). Paper-lane results are evidence-gathering only, NEVER promotion (protocol §8 — promotion would need a new owner-gated pre-registered protocol, which the agent never schedules).
verification (2026-07-10, read-only cross-check of docs/final-report.md §Holdout against docs/p5-holdout-protocol.md §5–§6): PASS on all four checks — (1) 13 verdicts present, 1 primary + 12 secondaries (final-report.md § "All 13 verdicts", table rows P, S1–S12); (2) variants-tried denominators carried: `variants_tried = 1` per run stated in the §Holdout preamble, lane burdens 177/92/144/177 + program-wide 13 stated verbatim in § "Secondaries: k = 2 of 12", and per-candidate Burden column in the ranked table; note the §Holdout verdict table itself has no per-row burden column — the burden lives in the ranked table + aggregate line; (3) window stated per subject ("Window used" column, incl. exact hourly UTC stamps); (4) hourly contingency disposition stated: NOT triggered, no NOT-EVALUABLE (§ "Contingencies, ambiguities, and mechanical notes").
blockers: none.
orders: acked=001–008, done=001–008 — unchanged this session. Inbox re-read at origin/main HEAD 2026-07-10T22:03Z before this write: no order newer than 008. No OWNER-ACTION items arose from the paper-lane work (paper-only: no accounts, no spend, no signups; DEGIRO stays a read-only manual owner benchmark per Q-0250 with deliberately no owner-action item).
⚑ needs-owner (six-field form — what / why / exact click / where / blocking? / fallback):
- (b) ENV SETUP SCRIPT / fresh-environment sessions die silently at provision without it (setup runs at cwd=/home/user with repos as subdirectories) / paste the contents of environments/setup-universal.sh into the environment's setup-script field and save / Claude environment config for this project / not blocking (current pinned env works) / keep spawning into the pinned env and treat any child with no heartbeat within 10 min as dead → respawn.
- (c) ALLOW AUTO-MERGE TOGGLE / auto-merge arm fails pending-side with "unstable status" while the repo toggle is off, so every merge needs an agent to poll-and-merge (reconfirmed on PR #36) / tick "Allow auto-merge" / GitHub → menno420/trading-strategy → Settings → General → Pull Requests / not blocking / continue REST squash-merge on green (the path #40–#43 all used, per ORDER 002 merged-on-green).
- (d) ARCHIVE DEAD GEN-1 SESSION / the gen-1 "ORDER 001 successor" session died at provision, emits no failure event, and still lists as active — misleading / open the project's session list, locate "ORDER 001 successor", archive it / claude.ai session list for this project / not blocking / ignore it — it consumes nothing.
next-update-by: 2026-07-17T23:59:00Z (weekly grading cadence, protocol §6 — first pass is expected FLAT: warm-up runs ~3 weeks)
notes: paper-lane dispatch close-out. Ledger discipline live: experiments/paper/ledger.md is append-only, every trade row must be committed before its outcome window opens (git timestamp = tamper evidence, late = MISS), and record paper-0001 honestly states the flat/warm-up opening — no position fabricated, no pre-lane bar informed any decision, no market data read. Grading is idempotent and cannot double-grade (re-runs are byte-identical no-ops; running it today prints WATCH + FLAT and writes nothing). docs/current-state.md carries the in-flight/shipped mirror of #40–#43. Forward-only git preserved; control/inbox.md read-only, untouched; no model identifiers in repo per session policy; timestamps from date -u only.
