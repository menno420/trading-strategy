# trading-lab · status
updated: 2026-07-10T03:16:30Z
phase: gen-2 running — P1 trend × hourly lane (QUEUE § Next item 3) complete; next per QUEUE § Next: item 4 P2 validation of the accumulated candidates
health: green (pytest green; bootstrap check --strict --require-session-log exit 0 on the lane card; sweep validated locally before push)
last-shipped: #19 — P1 trend-hourly lane: the daily trend grids (4 families × 177 variants) re-run on hourly bars × 8 tickers (committed cache, zero network), walk-forward OOS vs B&H at 5+1 bps, results doc + 32 sweep files + 32 ledger runs + lane wrap-up (claim deleted, QUEUE item 3 DONE, session-card amendment)
blockers: none
orders: acked=001 (= inbox ORDER 005) — done=005 items 1–3 (prior sessions); standing default in force (QUEUE § Next top-to-bottom): item 3 trend × hourly DONE this session via PR #19. ORDER 005 item 4 (holdout hardening) remains DEFERRED = QUEUE item 5. Inbox re-read at origin/main HEAD (64d1d06) 2026-07-10T03:16:30Z: no orders newer than ORDER 005.
lane-result: trend × hourly (QUEUE item 3) DONE — negative headline: 5/32 family × ticker lanes beat B&H OOS with 177 variants tried, over a SINGLE ~8.5-month OOS regime (2024-03 → 2024-11; the dominant caveat vs the decade-long daily OOS). MACD is worst on 5/8 tickers and negative on 6 — hourly churn (~80–210 position changes/yr on top variants) is cost-eaten at 6 bps/side. Survivors GOOGL-sma/ema/donchian (1.25/1.06/1.05 vs 1.01), AMZN-donchian (0.91 vs 0.61), META-macd (1.40 vs 0.49, unstable per-split) are P2 candidates only, NOT findings. Full honest read: docs/p1-trend-hourly-results.md.
⚑ needs-owner: (1) env setup script — paste environments/setup-universal.sh into the environment config (tested; evidence in docs/succession/ENVIRONMENT.md); (2) one-time: tick "Allow auto-merge" in repo Settings→General→Pull Requests (GraphQL arm still fails; merging via REST squash on green instead); (3) archive the dead "ORDER 001 successor" session (still listed active).
next-update-by: 2026-07-10T11:16:30Z
notes: forward-only git preserved; wrap-up batched into the lane PR per orders (no separate status PR). Holdout untouched: all 32 hourly ledger rows data_end = 2025-01-08 20:30 < HOLDOUT_START (loader-enforced + script assert), zero network fetches (committed hourly cache). No model identifiers in repo per session policy.
