[project]
name = "trading-lab"
version = "0.1.0"
description = "Autonomous trading-strategy research lab (research only - no live trading)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
