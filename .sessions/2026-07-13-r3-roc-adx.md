# 2026-07-13 — R3 slice 2: ROC momentum + ADX-filtered SMA sweep (dev-only)

> **Status:** `complete` — two new trend/momentum families built, tested,
> and swept over the full 8-ticker daily universe on the standard dev rail;
> honest outcome **0 PROMOTED / 2 KEEP (dev-candidate only) / 14 KILL** of
> 16 lanes. This card was born red (`in-progress`) and flipped `complete`
> as the deliberate last content change before push; the claim file is
> deleted in this same commit.

📊 Model: fable-5 · r3-roc-adx lane (worker session) · start 2026-07-13T01:08Z

💡 **Session idea:** filter-gated families keep shipping without a
committed control arm — `vol_filtered_trend` deliberately swept
`vol_filter ∈ {on, off}` so the gate's contribution was attributable, but
this lane's `adx_filtered_sma` grid swept only gated arms
(`adx_min ∈ {20, 25}`), so the META KEEP cannot be attributed to the ADX
gate vs the plain crossover it wraps (the near-zero-gate arm exists only in
a unit test). Adopt a convention: every gated/filter family's declared grid
must include the filter-off arm as a within-family baseline (anchor:
`_R3_ROC_ADX_AXES` in `src/trading_lab/sweeps.py` — add `adx_min: 0.0`;
test target: a `test_sweeps.py` check that any family whose axes include a
gate parameter also includes its neutral value), and have the sweep
summary JSON record the gated-vs-control OOS delta per ticker. Recent
cards' ideas (sweep-runner extraction, docs-gate advisory, in-flight decay
advisory) don't cover this.

## Why this session exists

ORDER 012 (control/inbox.md, owner night-run direct order, 2026-07-13)
instructs this seat: "TRADING RESEARCH: expand the backtest surface — new
strategies, new stocks/tickers, new indicators — every result recorded
honestly." Slice 1 (PR #81, r3-stoch-willr) covered the oversold-oscillator
reversion side; this slice covers the trend/momentum side with the two
most-cited families the library still lacks: time-series rate-of-change
momentum (`roc_momentum`) and an ADX regime filter (`adx_filtered_sma` —
the first Wilder ADX indicator in the lab). Both slot into the existing
long/flat conventions (signal at bar t, engine fills at bar t+1 open) with
modest pre-declared grids. Promotion is CLOSED post-holdout: mostly-KILL
was the expected outcome and is recorded as a first-class result.

## Work log

- 2026-07-13T01:08Z — clone hard-synced to origin/main HEAD `7365a4c`
  (verified byte-identical to `git ls-remote origin main`).
  `control/inbox.md` read at HEAD (read-only): newest order is **ORDER 012 ·
  2026-07-13T00:46:37Z · status: new** (owner night-run direct order) — this
  session executes its "TRADING RESEARCH: expand the backtest surface" lane.
  Collision check: `control/claims/` at HEAD contains only `README.md` +
  `2026-07-13-order-night-run.md` — no overlap with this scope. Branch
  `claude/r3-roc-adx`; this card + the claim
  `control/claims/2026-07-13-r3-roc-adx.md` are the born-red FIRST commit
  (`f105fd6`), pushed before any build work.
- 2026-07-13T01:12Z — commit `e0383e5`: strategies `roc_momentum`
  (ROC(lookback) = close/close.shift(lookback) − 1; long on ROC > `entry`
  strict, exit on ROC < `exit` strict, held in the `exit <= entry`
  hysteresis band between; `entry == exit == 0` recovers the classic
  time-series momentum rule; warm-up flat) and `adx_filtered_sma` (SMA
  crossover long only when fast > slow AND Wilder ADX(`adx_period`) >
  `adx_min` strict; ADX built from Wilder-smoothed ±DM over Wilder ATR —
  the repo's shared `wilder_atr`; first `2·adx_period` bars forced
  undefined, zero-range DX undefined — unconfirmable regime resolves
  against the strategy); registered in `STRATEGIES`/`DEFAULT_PARAMS` +
  `R3_ROC_ADX_FAMILY`; grids declared in `trading_lab.sweeps` BEFORE the
  sweep ran (12 variants/family: roc lookback {63,126,252} × entry
  {0, 0.05} × exit {−0.05, 0} with exit ≤ entry; adx fast {10,20} × slow
  {50,100,200} × adx_min {20,25}, fast < slow; adx_period NOT swept);
  11 new unit tests + 6 grid tests, all offline synthetic data.
- 2026-07-13T01:15Z — sweep run (`scripts/run_r3_roc_adx_sweep.py`,
  mirrors `run_r3_stoch_willr_sweep.py`: `load_ohlcv` default rail, dev
  bars 2010-01-04 → **2025-01-08**, costs 5 bps + 1 bp per side,
  walk-forward 1008/252 contiguous stitched OOS vs same-window same-cost
  B&H; 192 registered configs, program cumulative 1064 = 872 prior + 192).
  Honest verdicts (Round-2 KEEP/KILL rule; ORDER 007 t-stat at Bonferroni
  K=12 → min t 2.64 recorded per lane, informational only, promotion
  CLOSED):
  - roc_momentum: **7/8 KILL**; META KEEP (dev-candidate only, OOS 0.734
    vs 0.653, t=0.23). Tracks B&H closely everywhere (long most of the
    time on mostly-up tickers) and still loses net of costs on 7 of 8.
  - adx_filtered_sma: **7/8 KILL**; META KEEP (dev-candidate only, OOS
    0.700 vs 0.653, t=0.13). Worst lane GOOGL (−0.260 vs 0.720, t=−3.10)
    — the gate mostly just subtracts exposure.
  - Both KEEPs deep inside noise vs the 2.64 bar; NOT findings, nothing
    promoted. 16 sweep summaries in `experiments/sweeps/r3-roc-adx/`, 16
    ledger rows (one top-variant run per lane, `variants_tried=12`), index
    rebuilt via `trading_lab.ledger.rebuild_index()`. Commit `ca896f5`.
- 2026-07-13T01:17Z — PR **#82** opened READY (non-draft) with the full
  honest results table; no merge action by this session (auto-merge-enabler
  is the landing path).

## Previous-session review

⟲ The most recent prior card (`.sessions/2026-07-13-r3-stoch-willr.md`,
Status `complete`) landed PR #81 clean and is the template this slice
mirrored: born-red first commit, pre-declared grids, dev-rail sweep with
honest 2 KEEP-dev / 14 KILL verdicts, flip+claim-delete folded into one
final commit — correct discipline throughout, and its verify block's
claimed counts (250 passed, bootstrap exit 0) reproduce at this session's
start. Its 💡 (extract a shared `trading_lab.sweeprunner.run_lane` helper)
is now even more overdue: this session hand-copied the same ~200-line
sweep script a sixth time, changing only names and the burden constant —
exactly the drift risk that idea names. Endorsed as the top follow-up;
this card's own idea (committed filter-off control arms) is complementary,
not overlapping.

## Close-out

**Done:** on branch `claude/r3-roc-adx` (PR #82) —

1. Two new strategies + registration
   (`src/trading_lab/strategies/roc_momentum.py`, `adx_filtered_sma.py`,
   `__init__.py`).
2. Pre-declared Round-3 slice-2 grids (`src/trading_lab/sweeps.py`) +
   tests (`tests/test_strategies.py`, `tests/test_sweeps.py`).
3. Sweep script `scripts/run_r3_roc_adx_sweep.py` + 16 lane summaries
   under `experiments/sweeps/r3-roc-adx/` + 16 ledger rows + rebuilt
   `experiments/index.jsonl`.
4. This card (born-red first commit → flipped `complete` last) — claim
   file deleted in this same commit.

**Verify:**
`python3 -m pytest -q` → 271 passed.
`python3 bootstrap.py check --strict --require-session-log --session-log .sessions/2026-07-13-r3-roc-adx.md`
→ exit 0 after this flip. Integrity at close: dev rail only (every lane
`data_end = 2025-01-08`), holdout (SPENT) untouched, `unlock_holdout`
never passed, paper-lane files byte-untouched, `control/inbox.md` +
`control/status.md` byte-untouched, no promotion/finding language beyond
the recorded KILL/KEEP-dev verdicts, NO merge action taken by this
session.

**Next (guard recipe):** none owed for correctness. Follow-up candidates:
the sweep-runner extraction (slice 1's 💡, endorsed above) and the
filter-off control-arm convention (this card's 💡 — anchors and test
target named there). The two META KEEPs are dev-candidates only; any OOS
test of them is OWNER-GATED behind a new pre-registered protocol on
post-2026 data, per docs/research-round-2-results.md.

Session end: badge flipped `complete` in this final content commit before
push.
