# 2026-07-17 — Round 7 run: execute the two pre-registered slices (R7-A drawdown_reversion + R7-B high_proximity)

> **Status:** `complete` — Round 7 executed — 5 KEEP-dev / 25 KILL /
> 0 KILL-SIG of 30 lanes, 0 promoted, best informational t 1.195
> (BTC-USD `high_proximity`) vs the unchanged 2.638 bar; gate 14 PASS /
> 16 FAIL, 0 UNGRADEABLE; runtimes 9.0/9.4 s vs 900 s caps; program
> 5,055 → 5,415 configs. Results: `docs/research-round-7-results.md`.

📊 Model: opus-4.8 · round-7-run lane (coordinator-dispatched overnight worker, owner order 2026-07-17T22:39Z) · start 2026-07-17T22:52:46Z

💡 **Session idea:** the 30-lane per-lane results tables in
`docs/research-round-7-results.md` were transcribed by hand from the lane
JSONs + `summary.json` — a copy/transcription-risk surface that grows
every round. Promote a small deterministic `scripts/render_round_results.py`
that emits the results-doc markdown (slice-summary table, per-lane tables,
`reason_class` rollup, burden line) directly from a slice's
`experiments/sweeps/<SLICE>/summary.json` + lane JSONs, so results docs
become generated-and-diffable instead of hand-built. Anchor: the per-lane
table blocks in `docs/research-round-{6,7}-results.md`; test target: a
golden test that the generator reproduces the committed R7 tables
byte-for-byte from the committed JSONs. (Distinct from the round-6-run
card's shared-`trading_lab.lanerunner` idea — that dedupes runner code;
this dedupes results-doc transcription.)

## Why this session exists

Owner order 2026-07-17T22:39Z ("work on the trading strategies", landed as
`control/inbox.md` ORDER 018) unlocks executing the pre-registered Round-7
plan (`docs/research-round-7-plan.md`, badge `binding`, PLAN-ONLY until
now). This session adds the two families + grids + pinning/causality tests
in the first code commit (plan-before-outcome), then runs the two slices
top-down under the selection-fair gate on every lane, and writes
`docs/research-round-7-results.md`.

## Work log

- branch `claude/round-7-run` cut from origin/main HEAD `68fbd39`. Born-red
  FIRST commit: this card + ORDER 018 append to `control/inbox.md`.

## Previous-session review

⟲ PR #140 (`2026-07-17-fresh-start-cleanup.md`, 68fbd39 — the HEAD this
branch was cut from) — the owner-authorized fresh-start cleanup ahead of
the EAP read-only cutoff: it made `docs/current-state.md` the single
living ledger, stripped the auto-merge / "silence = consent" merge
doctrine and self-persistence/wake-chain doctrine from `CONSTITUTION.md`
and `docs/ROUTINES.md`, filled the trading-lab hard rails, and curated
`docs/NEXT-TASKS.md` (with Round 7 as a top next step). Those claims held
up against the tree I worked in: `current-state.md` was in fact the doc I
oriented from, `NEXT-TASKS.md` named Round 7 exactly as executed here, and
its docs-only / holdout-SPENT / no-`src/` claim matches — my Round 7 run
touched `src/trading_lab/` and sweeps but never the holdout or the
promotion path, consistent with the rails it wrote. One honest nit: the
card asserts "Local verify: `python3 bootstrap.py check --strict` exit 0",
yet it left `control/inbox.md` untouched by design — so its own green did
not exercise the `[inbox-order-grammar]` append gate, which is exactly
what later red on ORDER 018 in this branch (fixed in commit 90bee78).
Not a defect in its own diff, but a reminder that a docs-only card's
green does not cover the append-only inbox lane.

## Close-out

**Done** (6 commits):
- `df78ae0` — born-red FIRST commit: this card + ORDER 018 append to
  `control/inbox.md`.
- `f41b4f1` — `drawdown_reversion` + `high_proximity` families, R7 grids,
  pinning + causality tests (plan-before-outcome).
- `b61fb6c` — ran R7-A `drawdown_reversion` + R7-B `high_proximity` (30
  lanes / 360 configs), lane JSONs + summaries + run records + index.
- `9109819` — `docs/research-round-7-results.md` + `docs/current-state.md`
  (30 lanes graded, program 5,055 → 5,415).
- `90bee78` — `control/inbox.md` ORDER 018 grammar fields
  (priority/do/why/done-when) — clears the `[inbox-order-grammar]` red.
- (this commit) — close-out flip: badge → `complete`, idea/review/close-out
  slots resolved.

**Verify:**
- `python3 -m pytest -q` → 694 passed.
- `python3 bootstrap.py check --strict` → EXIT 0 at flip (the born-red
  hold clears when the badge flips to `complete`; the grammar red was
  fixed in `90bee78`).
- Integrity: the branch diff touches only `src/trading_lab/strategies` +
  `sweeps.py` + tests + `scripts/run_r7_*` + `experiments/sweeps/r7-*` +
  `experiments/runs` + `experiments/index.jsonl` +
  `docs/research-round-7-results.md` + `docs/current-state.md` +
  `control/inbox.md` (ORDER 018 only) + this card. Holdout never read,
  `unlock_holdout` never passed, no fetch, `experiments/paper/**`
  untouched, `control/status.md` untouched, no triggers, no
  broker/exchange code, `min_tstat` bar unchanged, promotion CLOSED /
  0 promoted. NO manual merge — the auto-merge enabler lands PR #141 on
  green.

**Next (guard recipe):** the 5 R7 KEEP-devs are dev-candidates ONLY, at
the bottom of the stability ladder — none has seen the R5-A/B/C battery,
so a stability round should target them before anyone cites them. Plus
this card's 💡 results-doc generator (`scripts/render_round_results.py`
with a golden byte-for-byte test) to end hand-transcription of per-lane
tables.
