# 2026-07-18 — Round 7C run: execute the pre-registered washout_recovery conjunction slice

> **Status:** `in-progress` — executing `docs/research-round-7c-plan.md`
> under owner order 2026-07-17T22:39Z; 15 lanes, 180 configs, program
> 5,415 → 5,595. Born-red hold until close-out.

📊 Model: opus-4.8 · round-7c-run lane (overnight worker, owner order 2026-07-17T22:39Z) · start 2026-07-18T00:27:47Z

## Why this session exists

Owner order 2026-07-17T22:39Z ("work on the trading strategies", landed as
`control/inbox.md` ORDER 018) authorizes executing the pre-registered
Round-7C plan (`docs/research-round-7c-plan.md`, badge `binding`, merged as
PR #144, PLAN-ONLY until now). Round 7C registers ONE genuinely new family
on the committed daily caches only — `washout_recovery`, the drawdown ×
high-proximity CONJUNCTION — asking whether the intersection of a
long-horizon washout AND a short-horizon recovery carries information that
R7-A (`drawdown_reversion`) and R7-B (`high_proximity`) each left on the
table when graded in isolation. This session adds the family + grid +
pinning/causality tests in the first code commit (plan-before-outcome),
then runs the 15-lane slice top-down under the selection-fair gate on every
lane, and writes `docs/research-round-7c-results.md`. POST-HOLDOUT,
DEV-ONLY: the holdout is SPENT and promotion is CLOSED — a KEEP anywhere is
a dev-candidate only, never a finding. The `min_tstat` bar (~2.638 at K=12)
is UNCHANGED.

## Work log

- branch `claude/round-7c-run` cut from origin/main HEAD (incl. #144, the
  R7C pre-registration). Born-red FIRST commit: this card.

💡 **Session idea:** the R7C grid (`_R7C_CONJUNCTION_AXES`), its
constraint set (`_R7C_CONJUNCTION_CONSTRAINTS`), and the `r7c_*` expansion
helpers are a fourth verbatim copy of the R6/R7 axes-plus-constraints-plus-
`product` idiom now living in `trading_lab.sweeps` — each round re-pastes
the same generator with a fresh prefix. Promote a single generic
`expand_axes(axes, constraints, family)` (deterministic `product` order,
constraint filter, unknown-family `ValueError`) that the per-round grids
call, so a new round registers only its axes dict + constraint lambda +
instrument tuple and inherits the tested expansion mechanics. Anchor: the
identical `combos = (dict(zip(keys, vals)) for vals in product(...))` block
repeated at every `_R*_*_variants` function; test target: a golden test
that the generic expander reproduces each committed round's variant list
exactly. (Distinct from the round-7-run card's results-doc-generator idea —
that dedupes results-doc transcription; this dedupes grid-expansion code.)

## Previous-session review

[[fill:]]

## Close-out

[[fill:]]
