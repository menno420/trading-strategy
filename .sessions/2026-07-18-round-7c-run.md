# 2026-07-18 — Round 7C run: execute the pre-registered washout_recovery conjunction slice

> **Status:** `complete` — Round 7C executed — washout_recovery
> conjunction: 1 KEEP-dev / 11 KILL / 3 KILL-SIG of 15 lanes, 0 promoted,
> best informational t 0.22 (TLT) vs the unchanged 2.638 bar; gate 2 PASS /
> 13 FAIL (1 KEEP demoted, GLD); the first R7 family to produce KILL-SIG
> lanes (AMZN −3.00, MSFT −3.72, QQQ −3.11 — significantly worse than B&H on
> high-drift names); runtime 9.3 s vs 900 s cap; program 5,415 → 5,595.
> Results: docs/research-round-7c-results.md.

📊 Model: opus-4.8 · round-7c-run lane (overnight worker, owner order 2026-07-17T22:39Z) · start 2026-07-18T00:27:47Z

## Why this session exists

Owner order 2026-07-17T22:39Z ("work on the trading strategies", landed as
`control/inbox.md` ORDER 018) authorizes executing the pre-registered
Round-7C plan (`docs/research-round-7c-plan.md`, badge `binding`, merged as
PR #144, PLAN-ONLY until now). Round 7C registers ONE genuinely new family
on the committed daily caches only — `washout_recovery`, the drawdown ×
high-proximity CONJUNCTION — asking whether the intersection of a
long-horizon washout AND a short-horizon recovery carries information that
R7-A (`drawdown_reversion`) and R7-B (`high_proximity`) each left on the
table when graded in isolation. This session adds the family + grid +
pinning/causality tests in the first code commit (plan-before-outcome),
then runs the 15-lane slice top-down under the selection-fair gate on every
lane, and writes `docs/research-round-7c-results.md`. POST-HOLDOUT,
DEV-ONLY: the holdout is SPENT and promotion is CLOSED — a KEEP anywhere is
a dev-candidate only, never a finding. The `min_tstat` bar (~2.638 at K=12)
is UNCHANGED.

## Work log

- branch `claude/round-7c-run` cut from origin/main HEAD (incl. #144, the
  R7C pre-registration). Born-red FIRST commit: this card.

💡 **Session idea:** the R7C grid (`_R7C_CONJUNCTION_AXES`), its
constraint set (`_R7C_CONJUNCTION_CONSTRAINTS`), and the `r7c_*` expansion
helpers are a fourth verbatim copy of the R6/R7 axes-plus-constraints-plus-
`product` idiom now living in `trading_lab.sweeps` — each round re-pastes
the same generator with a fresh prefix. Promote a single generic
`expand_axes(axes, constraints, family)` (deterministic `product` order,
constraint filter, unknown-family `ValueError`) that the per-round grids
call, so a new round registers only its axes dict + constraint lambda +
instrument tuple and inherits the tested expansion mechanics. Anchor: the
identical `combos = (dict(zip(keys, vals)) for vals in product(...))` block
repeated at every `_R*_*_variants` function; test target: a golden test
that the generic expander reproduces each committed round's variant list
exactly. (Distinct from the round-7-run card's results-doc-generator idea —
that dedupes results-doc transcription; this dedupes grid-expansion code.)

## Previous-session review

⟲ PR #144 (`r7c-plan`, merged) — landed `docs/research-round-7c-plan.md`
(badge `binding`): the washout_recovery drawdown × high-proximity
pre-registration this run executes — family, signal logic, the 12-variant
grid, registered null, decision rule, and the exact code + tests a run must
add before any run. **Did the run honor the pre-registration?** Yes,
end-to-end. The committed grid resolved to exactly 12 variants = 2×3×2 as
registered (the `W_dd × W_prox × band` axes with `W_prox < W_dd` enforced as
a constraint — no variant with proximity window ≥ drawdown window survived
expansion); instruments were the frozen `R7_INSTRUMENTS` tuple, so 12
variants × 15 lanes = 180 configs, program 5,415 → 5,595 exactly as the plan
projected. The registered **null** ("most lanes KILL — the conjunction
carries no information the components lacked") **held**: 11 KILL / 3 KILL-SIG
/ 1 KEEP-dev, best informational t 0.22 (TLT) far under the 2.638 bar, 0
promoted. The registered **mirror** ("tighter recovery bands out-grade wider
ones") did NOT clear — no monotone band effect, exactly as the plan predicted
it would fail to. **One honest nit (not a defect):** the plan's baseline null
anticipated KILLs but did not register the *sharper* outcome the run actually
produced — 3 KILL-SIG lanes (AMZN −3.00, MSFT −3.72, QQQ −3.11) where the
conjunction is *significantly worse* than buy-and-hold on high-drift names,
not merely uninformative; that is a slightly sharper null than the plan's
baseline expectation, harm rather than noise, and worth carrying forward as
its own registered hypothesis. Plan held end-to-end otherwise — grid,
constraint, instruments, config count, null, and mirror all as
pre-registered, no defect.

## Close-out

**Done** (5 commits):
- `243a71b` — born-red FIRST commit: this session card (`in-progress` hold).
- `b225cc2` — the `washout_recovery` family + R7C grid
  (`_R7C_CONJUNCTION_AXES`, `r7c_conjunction_variants`, `r7c_total_configs`)
  + pinning & causality tests (grid + counts pinned before any run).
- `a1e2350` — the R7C run: `scripts/run_r7c_*.py`, the 15-lane
  washout_recovery slice + artifacts under
  `experiments/sweeps/r7c-conjunction` + `experiments/runs` + `index.jsonl`.
- `96f5989` — `docs/research-round-7c-results.md` (15 lanes graded) + the
  `docs/current-state.md` pointer (program 5,415 → 5,595).
- (this commit) — close-out flip: badge `in-progress` → `complete`,
  headline + previous-session-review + close-out slots resolved.

**Verify:**
- `python3 -m pytest -q` → 713 passed.
- `python3 bootstrap.py check --strict` → EXIT 0 at flip (the born-red
  in-progress hold clears when the badge flips to `complete`).
- Integrity: the branch diff touches only the R7C `washout_recovery`
  family / grid / tests + `scripts/run_r7c_*` + `experiments/sweeps/
  r7c-conjunction` + `experiments/runs` + `index.jsonl` +
  `docs/research-round-7c-results.md` + `docs/current-state.md` + this
  session card. The holdout was never read, `unlock_holdout` never passed,
  no fetch, `experiments/paper/**` untouched, `control/status.md` untouched,
  no triggers, no broker / exchange code, the `min_tstat` bar unchanged,
  promotion CLOSED / 0 promoted. NO manual merge — the auto-merge enabler
  lands PR #145 on green.

**Next (guard recipe):** the R7-C KILL-SIG lanes (AMZN −3.00, MSFT −3.72,
QQQ −3.11) are the program's first conjunction-*harm* evidence — the
intersection of a washout state AND a recovery state is significantly worse
than buy-and-hold on high-drift names, not merely uninformative. A future
interaction round should register the INVERSE conjunction (washed-out AND
still-falling — a long-horizon washout with NO short-horizon recovery) with
the same plan-before-outcome discipline, to test whether the harm is the
recovery leg or the washout leg. Separately, the single TLT KEEP-dev (t 0.22,
demoted below the bar but the family's only non-KILL) joins the
low-vol-survivorship cluster the stability battery should target BEFORE
anyone cites it — a low-vol Treasury lane surfacing as the best-of-family is
exactly the survivorship pattern the battery exists to stress.
