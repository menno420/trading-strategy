# 2026-07-10 — P4 cross-instrument transfer validation

> **Status:** `complete` — scoped to the landed work: heartbeat (claim +
> card, PR #27) and the P4 delivery (this PR, QUEUE item 7): 99 frozen-
> param transfer backtests, 13/13 TRANSFER-FAILED, results doc + ledger
> rows + per-subject summaries + QUEUE items 7 (DONE) / 8 (open). Session
> wrap-up (status ender + claim release) follows in a later PR.

📊 Model: withheld per session policy · p4-transfer lane · start 2026-07-10T04:29:59Z

💡 **Session idea:** P4 cross-instrument transfer validation of the 13 P2
subjects (the 12 UNVALIDATABLE-PRE-HOLDOUT candidates plus the one
PROMOTED-TO-FINDING, AAPL-donchian — the plan requires survivors to
transfer too): freeze each subject's params verbatim from its P1 sweep JSON
(`top_full_period_variant`), run them unchanged on every OTHER instrument
in the same timeframe's committed cache (daily: the other 8 of
{AAPL,MSFT,NVDA,GOOGL,AMZN,META,GLD,SLV,BTC-USD}; hourly: the other 7
UNIVERSE tickers — no BTC-USD hourly cache exists), full pre-holdout
window via `load_ohlcv`, engine defaults (5+1 bps/side, t+1-open fills),
vs buy-and-hold on the same series. Pre-registered verdict rule per
subject (written in the script docstring before running): count transfer
instruments where strategy Sharpe > B&H Sharpe; TRANSFER-SUPPORTED if
≥ 2/3, TRANSFER-WEAK if ≥ 1/3 and < 2/3, TRANSFER-FAILED if < 1/3. A
transfer verdict never promotes anything to finding — it only sharpens
the prior for the sealed-holdout P5 test. Zero re-tuning, zero new
variants (`variants_tried = 1` per row); holdout untouched. First step:
this heartbeat PR (claim + card → READY PR → tests + substrate-gate →
landed on green).

## Previous-session review

QUEUE items 1–6 are all DONE and merged (last: holdout enforcement
hardening PR #24 and the collaboration-model port PR #25, wrap-up #26 —
lane `holdout-collab`). P2 verdicts (docs/p2-validation-results.md): 1
PROMOTED-TO-FINDING (AAPL-donchian daily), 1 KILLED (GOOGL-pullback), 12
UNVALIDATABLE-PRE-HOLDOUT — those 12 have zero unconsumed pre-holdout bars
in their home instrument×timeframe, which is exactly why cross-instrument
transfer is the next falsification lever that doesn't touch the holdout.
Overlap check at claim time: `claims/` held only its README; zero open PRs
on the repo. Known walls inherited: born-red cards cannot merge — badge is
`complete` scoped to the landed phase; new docs need a Status badge + a
link from a reachable doc; READY-never-draft, merge on green; auto-merge
arm fails two ways ("unstable status" pending / "already in clean status"
green) — REST squash on green is the fallback that has fired for every
merge to date; poll checks via the GitHub MCP `pull_request_read`
get_check_runs (raw curl to api.github.com hangs, exit 143); tag pushes /
branch deletion are 403 for agents; all bar loads via
`trading_lab.data.load_ohlcv` (holdout rail + Yahoo proxy workaround);
ledger refuses `data_end >= 2025-01-09` without the P5-only unlock marker.

## Work log

- 2026-07-10T04:29:59Z — heartbeat/skeleton: claimed the lane
  (`claims/p4-transfer__multi__daily-hourly.md`), session card (this
  file), branch `session/2026-07-10-p4-transfer`, READY PR (#27), landed
  via REST squash on green (auto-merge arm hit the known "unstable
  status" wall — pending checks).
- 2026-07-10T04:33:59Z — P4 delivery (QUEUE item 7), branch
  `p4-transfer-results`: `scripts/run_p4_transfer.py` (pre-registered
  protocol + ≥2/3 / ≥1/3 verdict rule in the docstring BEFORE running;
  frozen params pulled from the P1 sweep JSONs and cross-checked against
  the pre-registered list — all 13 matched); 99 backtests (8 daily
  subjects × 8 transfer instruments + 5 hourly × 7), each ledgered with
  `variants_tried=1`, index rebuilt, per-subject summaries under
  `experiments/sweeps/p4-transfer/`; audit: all 99 rows `data_end`
  ≤ 2025-01-08, zero `holdout_unlocked` markers. Result: **13/13
  TRANSFER-FAILED** (13/99 pairs beat B&H on Sharpe; best subject 2/8;
  the P2-promoted AAPL-donchian failed 1/8). Deliberate recorded
  deviation: full-period frozen-param protocol instead of walk-forward
  (degenerate for a single-variant grid), following the P2 precedent.
  `docs/p4-transfer-results.md` written (badge + inbound link from
  p2-validation-results.md); QUEUE item 7 marked DONE, item 8 (P5
  final-report prep, ⚑ owner may want to gate the unlock) appended open.
