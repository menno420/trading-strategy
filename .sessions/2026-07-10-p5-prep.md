# 2026-07-10 — P5 prep: holdout protocol + final-report skeleton

> **Status:** `complete` — scoped to the full session: heartbeat (claim +
> card, PR #30 READY), the P5-prep delivery (docs/p5-holdout-protocol.md
> + docs/final-report.md + QUEUE item 8 marked PREP DONE), and this
> wrap-up (status overwrite, card close-out, claim released) — all
> riding PR #30. Holdout sealed throughout. End 2026-07-10T04:53:30Z.

📊 Model: withheld per session policy · p5-prep lane · start 2026-07-10T04:46:22Z

💡 **Session idea:** QUEUE item 8, PREP portion only. Write (a) the
pre-registered one-shot holdout evaluation protocol
(`docs/p5-holdout-protocol.md`): subjects fixed now (primary = the sole
P2-promoted finding AAPL-donchian 15/5 daily; secondaries = every
P2/P4-ledgered candidate with params frozen verbatim from the P1 sweep
JSONs), engine settings (t+1-open, 5+1 bps/side, B&H benchmark per
subject), the exact holdout window (2025-01-09 → data end), verdict rules
written BEFORE anyone sees a holdout number, the mechanical unlock steps
per docs/holdout-enforcement.md (owner-gated), and one-shot reporting
rules (variants-tried denominators, no post-read tuning ever). And (b)
the ranked final-report skeleton (`docs/final-report.md`) with every
pre-holdout section fully written from the ledgered result docs — the
P2-promoted finding, the 13/13 P4 transfer failures and all negative
results as first-class content — plus a clearly-empty "Holdout results
(P5 — pending unlock)" section. ABSOLUTE RAIL this session: the holdout
stays SEALED — no `holdout_unlocked` marker, no bar dated ≥ 2025-01-09
fetched/loaded/scored, every touched ledger row `data_end ≤ 2025-01-08`
(this session runs zero backtests). The protocol doc does NOT unlock
anything; the unlock is an explicit owner click (⚑).

## Previous-session review

QUEUE items 1–7 all DONE and merged (last: P4 transfer, PRs #27/#28/#29,
13/13 TRANSFER-FAILED). Item 8 is the sole open item. Overlap check at
claim time: `claims/` held only its README; zero open PRs on the repo.
Known walls inherited: born-red cards cannot merge — badge `complete`
scoped to the landed phase; new docs need a Status badge in the first 12
lines + a link from a reachable doc; READY-never-draft, merge on green;
auto-merge arm fails two ways ("unstable status" pending / "already in
clean status" green) — REST squash on green is the fallback that has
fired for every merge to date; poll checks via GitHub MCP
`pull_request_read` get_check_runs (raw curl to api.github.com hangs,
exit 143); tag pushes / branch deletion are 403 for agents; NEVER edit
control/inbox.md.

## Work log

- 2026-07-10T04:46:22Z — heartbeat: claimed the lane
  (`claims/p5-prep__report__multi.md`), session card (this file), branch
  `p5-prep-holdout-protocol-and-report-skeleton`, READY PR opened (#30).
  Auto-merge arm attempted once at creation: known wall, pending-side
  ("The pull request is in unstable status (required checks are
  failing). Fix the failing checks before enabling auto-merge.") — REST
  squash on green is the landing path.
- 2026-07-10T04:52:00Z — deliverables, same branch:
  `docs/p5-holdout-protocol.md` (pre-registered one-shot protocol:
  closed 13-subject list, frozen params verbatim from the P1 sweep
  JSONs with per-row source citations, engine defaults, holdout window
  2025-01-09 → data end with hourly contingency, verdict rules fixed
  before any holdout number, owner-gated mechanical unlock steps,
  one-shot reporting rules) and `docs/final-report.md` (ranked
  pre-holdout skeleton: methodology summary, ranked 15-row table with
  burdens, the AAPL-donchian finding, 13/13 P4 transfer failures and
  all negative results first-class, §Holdout empty pending unlock).
  QUEUE item 8 marked PREP DONE with links to both docs (reachability
  for substrate-gate); unlock + evaluation left as the explicit
  owner-gated remainder. Zero backtests run, zero data loaded, holdout
  untouched. Local verify before push: `python3 -m pytest -q` 133
  passed; `python3 bootstrap.py check --strict` all checks passed.
- 2026-07-10T04:53:30Z — session wrap-up, same branch/PR: inbox re-read
  at origin/main HEAD (4af1861) — no orders newer than ORDER 005;
  `control/status.md` overwritten (P5 prep done, holdout unlock pending
  owner; all 4 open ⚑ items carried forward, the unlock ⚑ now pointing
  at docs/p5-holdout-protocol.md; next-update-by
  2026-07-10T12:53:30Z); this card closed with badge `complete` scoped
  to the full session; claim `claims/p5-prep__report__multi.md`
  released (deleted). Landing: REST squash on green (arm hit the known
  pending-side wall at creation).
