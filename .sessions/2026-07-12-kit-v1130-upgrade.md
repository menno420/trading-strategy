# Session 2026-07-12 — substrate-kit upgrade v1.12.1 → v1.13.0

> **Status:** `complete`

Intent: upgrade the vendored substrate-kit from v1.12.1 to v1.13.0 (dead boot-pointer fix, boot-wired docs/SKILLS.md skill index, playbook-grade skill bodies + advisory grounds checker) via the release-asset path, sha256 three-way verified. Distribution only — no lane domain work.

- **📊 Model:** fable-5 · high · kit-upgrade

## What shipped

- Vendored `bootstrap.py` v1.12.1 → v1.13.0; sha256 three-way verified (asset = release.json = kit tag 828e450 dist, `982b2667…0959`, 735,972 bytes).
- Previous dist banked to `.substrate/backup/bootstrap-1.12.1.py`; all pre-existing banks byte-identical.
- `upgrade --apply-docs` applied the template-improved `CONSTITUTION.md` (skill-index pointer).
- Manual merge of the diverged `docs/AGENT_ORIENTATION.md`: dead `.claude/CLAUDE.md` boot pointer (the file never existed in this repo) → `CONSTITUTION.md`; `docs/SKILLS.md` wired into the planted-doc router.
- `docs/SKILLS.md` replanted (was missing). Carve-out scan: 0 found on both kit-owned workflows.
- Verify: `python3 -m pytest -q` 229 passed; `python3 bootstrap.py check --strict` green except this card's own designed born-red hold.

## Session enders

- 💡 **Session idea:** the upgrade report classes `docs/AGENT_ORIENTATION.md` as "diverged — manual merge" but the tool already computes and prints the exact template delta; a `upgrade --apply-docs --merge-diverged` mode could apply hunks that touch only template-owned lines (like a dead pointer swap) and leave truly conflicting hunks flagged — this session's manual merge was a mechanical replay of the printed diff, ideal automation fodder.
- ⟲ **Previous-session review:** the v1.12.1 upgrade session (PR #63) ran the playbook cleanly — claim at open, deleted in flip, honest report. One miss it left behind: its report noted the substrate-gate template delta as "kit-side template evolution" without stating whether any action was pending; this session had to re-derive that nothing was outstanding. Improvement: upgrade reports should end with an explicit "outstanding actions: none / list" line so the next distribution worker inherits a verdict, not a clue.
