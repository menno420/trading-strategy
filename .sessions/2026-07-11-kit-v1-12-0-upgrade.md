# 2026-07-11 — kit upgrade: substrate-kit v1.11.0 → v1.12.0

> **Status:** `complete`

📊 Model: fable-5 · kit-distribution seat (owner directive Q-0261.3) · start 2026-07-11

💡 **Session idea:** The v1.12.0 `--apply-docs` rerun rewrites
`.substrate/upgrade-report.md` and (fix kit #176, verified live here) now
re-emits the "## Carve-out scan" section — but the rewritten report is
indistinguishable from the original at a glance. Kit idea: stamp each report
section with the generating command (`by upgrade` / `by --apply-docs` + time)
so a post-hoc rewritten report is self-evidently a rewrite — cheap provenance
for exactly the "did the rerun eat a section?" audit this wave had to do by
hand against the websites #101 regression.

## What happened

Vendored-kit upgrade v1.11.0 → v1.12.0 via the canonical release-asset path:
sha256-verified `bootstrap.py` asset
(`77c00b811429e1b526ccc7e0dcf597435c11048e16a67edba6050f516ad5e1f8`, 689586
bytes, tag v1.12.0 → `b310abaf`, release run 29160292286) + `release.json`
(`617dca357c06403cbdcf274b3f1a88a48b1d4a0f282aa190b9d0a07a913fe600`, 449
bytes), staged as `bootstrap.py.new`, `python3 bootstrap.py.new upgrade`
(staging inputs self-cleaned). Payload: substantive session-card auto-draft
(reflog subjects + carried-forward pointer), HANDOFF/SessionStart auto-derived
evidence trail, planted-orientation boot-set trim, unadopted auto-draft
advisory (bare strict lane only), carve-out scanner three-way compare fix
(kit #210). Kit upgrade ONLY — no domain work; `control/inbox.md` /
`control/status.md` / `tests.yml` untouched. Same-day wave as fleet-manager
#79 and superbot-games #51.

## Work log

- Pre-upgrade tree at v1.11.0 (vendored dist sha256 `c339bd6a…`, matching the v1.11.0 release asset), synced to origin/main `3172b431`.
- Backups: exactly ONE new bank, `.substrate/backup/bootstrap-1.11.0.py` (`c339bd6a2eb3a139dd0106d5fd3873eb2d067f79723fccb5781d4e72a74a8d29`, byte-equal to the pre-upgrade vendored dist). Pre-existing banks byte-untouched before/after: `bootstrap-1.1.0.py` `c12a2f55…`, `bootstrap-1.10.0.py` `ba69fc5c…`, `bootstrap-1.10.1.py` `fbe83ce3…`, `bootstrap-1.7.0.py` `00f4f4cd…`, `bootstrap-1.7.1.py` `2aa4fedd…`, `bootstrap-1.8.0.py` `28c5dcb6…`, `bootstrap-1.9.0.py` `55181082…`, and the audit bank `substrate-gate.pre-regen-4f50eb4d.yml` `4f50eb4d…`.
- Carve-out scanner (three-way compare, kit #210) — verbatim: `upgrade: carve-out scan: .github/workflows/substrate-gate.yml — ran, 0 found` + `upgrade: kept: .github/workflows/substrate-gate.yml (kit-owned, already current)`. Zero phantoms, NO new pre-regen bank, gate template unchanged v1.11→v1.12 — matching fleet-manager #79 and superbot-games #51. The v1.11.0 wave's false positive is gone.
- Planted docs: 21 classified — 12 unchanged, 7 consumer-edited, `docs/AGENT_ORIENTATION.md` diverged (template boot-set trim vs host edits → manual merge, LEFT LANE-OWED), `CONSTITUTION.md` template-improved + consumer-untouched → applied via `upgrade --apply-docs` (condensed, +20/−29 lines). Nothing host-owned overwritten.
- apply-docs report-rewrite check (kit #176, the websites #101 regression): after `--apply-docs`, `.substrate/upgrade-report.md` STILL carries the "## Carve-out scan" section with the verbatim scan line — fix verified live, no hand-restore needed.
- `.substrate/claude/CLAUDE.md` staged copy updated (+8/−5); still NO live root `CLAUDE.md` in this repo — the staged copy stays invisible to workers until the lane adopts it.
- guard-fires: exactly ONE new `.substrate/guard-fires.jsonl` line — the designed born-red session-log fire from the pre-flip `check --strict` (ts `2026-07-11T17:16:10+00:00`, posture `blocking`, this session's own card); the post-flip clean check appended nothing (dedupe held earlier duplicates). Committed in this flip commit per precedent.
- `check --strict` pre-flip → exit 1, only red the designed hold: "check: HOLD (by design): session card .sessions/2026-07-11-kit-v1-12-0-upgrade.md declares an in-progress Status — the born-red session gate holds the merge red until the card flips complete. This red is the designed hold, not a defect; nothing to investigate." (plus the same card's missing-markers advisory — same designed in-progress state, no other findings).
- CI: card-only head `8e63f98` — substrate-gate run 29161273113 designed-RED (born-red hold), tests run 29161273117 GREEN. Payload head `9d7cf3a` — substrate-gate run 29161311684 designed-RED, tests run 29161311683.
- `control/inbox.md` + `control/status.md` untouched; claim file deleted IN this flip commit (the #56 lesson).

## Previous-session review

⟲ Previous-session review: the 2026-07-11 kit-upgrade-v1.11.0 session (#60) is the reason this one was near-mechanical — its card carried the backup-hash table, the verbatim designed-HOLD line, the landing constraints (auto-merge OFF → MCP squash, branch auto-delete ON, claim-deleted-in-flip), and — exactly as its own review recommended — it recorded the adopter-side symptom of a kit-side change (the carve-out false positive) so this wave could recognize the three-way-compare fix as designed on first contact. What it could have done better: its close-out re-listed the same three lane-owed follow-ups #57 had already listed, with no aging signal — a lane-owed item restated verbatim across consecutive kit waves is drifting toward permanent. Workflow improvement: when a lane-owed follow-up appears a second consecutive wave, mark it `(2nd wave)` and route it to the fleet manager's attention line instead of only re-listing it (this card starts that convention below).

## Close-out

- Follow-ups owed to the repo's own lane (NOT done here, per distribution-seat hard scope) — now **3rd wave** for the first three: (1) `control/status.md` kit heartbeat line — bump to v1.12.0; (2) still no live root `CLAUDE.md` (kit copy staged at `.substrate/claude/CLAUDE.md`); (3) `docs/CAPABILITIES.md` landing-constraints entry still owed; NEW (4) `docs/AGENT_ORIENTATION.md` diverged — manual merge of the v1.12.0 boot-set trim (template delta preserved in `.substrate/upgrade-report.md`).
- Merge path: repo auto-merge toggle OFF → GitHub MCP `merge_pull_request` SQUASH on green, matching #51/#54/#55/#57/#60 precedent.
- Next session should know: v1.12.0 in tree; upgrade-report carve-out section survives `--apply-docs` (kit #176 verified); carve-out scanner three-way fix live (0 found, no new bank); AGENT_ORIENTATION manual-merge delta is in the upgrade report.
