# 2026-07-18 — Round 7D pre-registration plan (cross-sectional drawdown ranking on XSEC-14)

> **Status:** `complete` — Round 7D pre-registered (PLAN ONLY) —
> docs/research-round-7d-plan.md (binding): xsec_drawdown cross-sectional
> drawdown ranking on XSEC-14, 6-config portfolio lane (L{63,126,252}×k{2,3},
> rebalance frozen 21; configs=variants=6), program 5,595 → 5,601; running is a
> future slice; dev-only, promotion CLOSED.

📊 Model: opus-4.8 · r7d-plan lane (overnight worker, owner GO 2026-07-18T13:47Z / menu #02) · start 2026-07-18T15:16Z

## Why this session exists

Menu #02 (overnight menu): pre-register the FIRST cross-sectional PORTFOLIO
family that ranks the basket on DRAWDOWN DEPTH — Round 7D, `xsec_drawdown` on
the committed XSEC-14 basket, plan-before-outcome. The program's two burned
cross-sectional lanes both rank the cross-section on trailing point-to-point
RETURN — `xsec_momentum` (long winners) and `xsec_reversal` (long losers, 6/6
KILL on this basket in Round 3). No cross-sectional lane has ever ranked on
drawdown STATE (depth from a trailing peak, `close/rolling(L).max()-1`), even
though drawdown is the program's only consistently-observed effect
(retrospective §f) and single-name drawdown reversion mostly nulled in R7-A.
This session writes the BINDING pre-registration
(`docs/research-round-7d-plan.md`, badge `binding`): the `xsec_drawdown` family,
its signal logic, the 6-variant grid (`L`{63,126,252} × `k`{2,3}), the
registered null hypothesis, the portfolio decision rule, and the exact code +
tests a future run session must add BEFORE any run. PLAN ONLY — this session
runs NOTHING, commits no grid to `sweeps.py`, and touches no strategy /
backtest code; executing R7-D is a future session's separately claimed slice.
Build-then-review: implement, open PR READY, leave the card `in-progress` for
the owner's correctness check before flip.

## Work log

- branch `claude/research-round-7d-plan` cut from origin/main HEAD f8008d1
  (#146; program cumulative 5,595). Born-red FIRST commit: this card.

💡 **Session idea:** stand up a small `docs/cross-sectional-ranking-index.md` —
a one-row-per-signal table of every RANKING KEY the portfolio lane has scored
the XSEC-14 cross-section on (trailing return → `xsec_momentum`/`xsec_reversal`;
drawdown depth → `xsec_drawdown` once R7-D runs), with the ranking direction,
frozen cadence, K, and standing verdict per row. The portfolio lane's whole
degree of freedom is "what do we rank on, and which tail do we hold" — a
ranking-key index makes the unscored keys obvious (e.g. rank on trailing
volatility, on distance-from-moving-average, on realized-skew) and stops the
same key being re-litigated as "new family vs variant" after outcomes, exactly
as the interaction-family index does for conjunctions. Anchor: the `Declared
adjacency` blocks in `docs/research-round-{3,7d}-plan.md` and the
`R3_XSEC_EXPANDED_*` / `R7D_XSEC_DRAWDOWN_*` symbols in `sweeps.py`. (Distinct
from the strategy catalog's family index: this rolls up ranking KEYS within the
one portfolio family class, not families across classes.)

## Previous-session review

⟲ PR #145 (`round-7c-run`, merged) — executed the pre-registered Round-7C
`washout_recovery` conjunction slice (drawdown × high-proximity) on the daily
caches: 15 lanes graded, **1 KEEP-dev (TLT) / 11 KILL / 3 KILL-SIG**, 0
promoted, best informational t 0.22 vs the unchanged 2.638 bar (program
5,415 → 5,595). It was the first R7 family to go outright value-destroying —
the AMZN −3.00 / MSFT −3.72 / QQQ −3.11 KILL-SIG lanes are *significantly
worse* than buy-and-hold on high-drift names, not merely uninformative.
**Is its discipline the pattern this R7-D plan sets up for its own future
run?** Yes — R7-C is the exact template this plan pre-commits to. It merged
its `binding` plan (#144) BEFORE running, added the family + grid + pinning/
causality tests in the first code commit so the variant list was pinned
before any outcome, gated every one of the 15 lanes under the selection-fair
bar, and kept promotion CLOSED with the holdout untouched. R7-D's Close-out
**Next** slot registers precisely that sequence for the xsec_drawdown run —
family + `_R7D_XSEC_DRAWDOWN_AXES`/variants/total + pinning & causality tests
first, run + results-doc after — so the plan-merged-before-run, gate-every-
lane, causality-tested cadence carries forward verbatim. **One honest nit
(not a defect):** R7-C's registered baseline null anticipated KILLs but did
not pre-register the *harm* tail it actually produced (the 3 KILL-SIG lanes),
so the sharper "significantly worse than B&H" outcome arrived unregistered —
a lesson this R7-D plan should absorb by registering not just "most lanes
KILL" but the possibility that ranking the basket on drawdown depth
*mis-allocates* into the deepest-drawdown names and underperforms B&H
significantly, not merely nulls. Discipline otherwise clean, no defect.

## Close-out

**Done** (3 commits):
- `a4d883a` — born-red FIRST commit: this session card (`in-progress` hold).
- `ed7637e` — the R7D pre-registration `docs/research-round-7d-plan.md`
  (badge `binding`) + the `docs/current-state.md` program pointer
  (5,595 → 5,601).
- (this commit) — close-out flip: badge `in-progress` → `complete`,
  headline + previous-session-review + close-out slots resolved.

**Verify:**
- `python3 -m pytest -q` → 713 passed.
- `python3 bootstrap.py check --strict` → EXIT 0 at flip (the born-red
  in-progress hold clears when the badge flips to `complete`).
- Integrity: the branch diff touches only `docs/research-round-7d-plan.md` +
  the `docs/current-state.md` pointer + this session card. PLAN ONLY — no
  code / grid / run / verdict / holdout / paper / control-status / trigger
  changes; no `sweeps.py` grid committed, no strategy or backtest code
  touched, the holdout never read, `min_tstat` bar unchanged, promotion
  CLOSED. NO manual merge — the auto-merge enabler lands PR #147 on green.

**Next (the R7-D RUN slice):** a future separately-claimed session executes
the run this plan pre-registers. In the FIRST code commit (plan-before-
outcome): add `strategies/xsec_drawdown.py` with `generate_weights` (rank the
XSEC-14 basket on trailing drawdown depth `close/rolling(L).max()-1`, hold the
K names by ranking), register it in `PORTFOLIO_STRATEGIES`, and add
`sweeps._R7D_XSEC_DRAWDOWN_AXES` (`L`{63,126,252} × `k`{2,3}, rebalance frozen
21) + its `_variants` (6) + `_total` config helpers, plus the pinning &
causality tests that pin the 6-variant grid before any run. Then run
`scripts/run_r7d_xsec_drawdown.py` and land `docs/research-round-7d-results.md`
with every lane graded under the selection-fair gate. Mirror the R3
xsec-expanded runner (`R3_XSEC_EXPANDED_*`) for the cross-sectional ranking
mechanics and the R7-C run mechanics (`scripts/run_r7c_*`, `experiments/sweeps`
+ `experiments/runs` + `index.jsonl` artifacts) for the gate-every-lane,
holdout-untouched, promotion-CLOSED run discipline.
