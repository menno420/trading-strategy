# 2026-07-13 — R4-A: KILL-SIG verdict class + retroactive re-grade

> **Status:** `complete` — Round-4 slice R4-A landed exactly as
> pre-registered (`docs/research-round-4-plan.md` § R4-A, merged before
> this session started): a third sweep verdict **KILL-SIG** (iff
> `tstat ≤ −min_tstat` at the lane's own recorded K — the same Bonferroni
> bar, mirrored, zero new arithmetic) plus a NON-DESTRUCTIVE retroactive
> re-grade of all 302 committed r3 per-lane summaries into one new report
> artifact. Honest result: **58 KEEP / 238 KILL / 4 KILL-SIG /
> 2 UNGRADEABLE** — the hypothesis ("a small set, 1–3, of
> significantly-harmful lanes") qualitatively confirmed, quantitatively
> missed by one (4 members). Every r3 file byte-untouched; no backtest
> ran; bar unchanged; holdout SPENT; promotion CLOSED. This card was born
> red (`in-progress`) and flipped `complete` as the deliberate last
> content change before push; the claim file is deleted in this same
> commit.

📊 Model: fable-5 · trading-research (worker session) · start 2026-07-13T09:34:09Z

💡 **Session idea:** cited round aggregates should be machine-reconciled,
not prose. This session hit a live discrepancy: the pre-registered plan
says "~312 existing summary JSONs" (from the PR #95 extended-round tally)
but `experiments/sweeps/r3-*/` actually holds 302 per-lane files (300 with
verdicts) — harmless here because R4-A's scope is defined by the glob, but
the same drift in a KEEP count would be a real error. Proposal: a `tally`
check (bootstrap subcommand or test) that recomputes every round-level
aggregate cited in `docs/research-round-*-results.md` and the round plan
(lane counts, KEEP/KILL/KILL-SIG splits, max t-stat) directly from the
committed summary JSONs and fails on mismatch, with a small
machine-readable block in each doc pinning which glob each number is
derived from. Not covered by the standing stack (preregistered_in ordering,
bench_difficulty, round manifest, sweep-runner extraction): those verify
commitment ordering or grade results; this one verifies that the *prose
numbers* agents and the owner actually read are reproducible from the
artifacts underneath them.

## Why this session exists

Round 4 slice R4-A (`docs/research-round-4-plan.md` @ main, pre-registered
and merged before this session started): the grading vocabulary has no word
for "significantly harmful" — TSLA `rsi_mean_reversion`'s stitched OOS
t = −3.01 (PR #91 card) crosses the 2.64 Bonferroni bar in the NEGATIVE
direction yet is recorded as just one KILL among 248. R4-A pre-registers a
third verdict **KILL-SIG** (iff `tstat ≤ −min_tstat(K)` at the lane's own
recorded K — the same bar, mirrored, zero new arithmetic) and a retroactive,
NON-DESTRUCTIVE re-grade of all merged r3 sweep summaries: original JSONs
byte-untouched, one separate report artifact + doc table. A KILL-SIG is
evidence AGAINST the lane, never an "invert the loser" signal. Owner mandate:
ORDER 012 generative rung; bar unchanged (`trading_lab.promotion.min_tstat`,
~2.64 at K=12); holdout SPENT; promotion CLOSED.

## Work log

- 2026-07-13T09:34Z — branch `claude/r4-killsig` cut from origin/main HEAD
  (`135101c`, the merged Round-4 plan). Collision check:
  `control/claims/` at HEAD holds only README + morning-tally +
  order-night-run — no overlap. Born-red FIRST commit `2befd2c` (this card
  `in-progress` + claim `control/claims/2026-07-13-r4-killsig.md`), pushed
  before any implementation; PR **#100** opened READY (non-draft)
  immediately after, Before/After first, citing the plan § R4-A, the
  PR #91 TSLA card, and ORDER 012. No PR template exists in the repo, so
  the body is structured free-form (same as PR #99).
- 2026-07-13T09:40Z — commit `117f380`, the whole slice:
  - `src/trading_lab/promotion.py`: pure total
    `classify_verdict(keep, tstat, min_tstat)` → `KEEP`/`KILL`/`KILL-SIG`
    (+ `SWEEP_*` constants). KILL-SIG only for non-KEEP lanes with
    `tstat ≤ −min_tstat`; None/NaN/non-numeric degrade to plain KILL —
    the function never invents a significance claim.
  - `scripts/run_r4_killsig_regrade.py` (docstring-first, POST-HOLDOUT
    DEV-ONLY banner, ORDER 012 + pre-registration pointer): read-only walk
    of every `experiments/sweeps/r3-*/` per-lane JSON → ONE report
    `experiments/sweeps/r4-killsig-regrade/summary.json` (rows: sweep,
    instrument, strategy, timeframe, original verdict, tstat, min_tstat,
    new verdict, source_file). Lanes with no recorded verdict/t-stat (the
    2 XSEC bookkeeping composites) recorded `UNGRADEABLE`, counted, never
    recomputed. `experiments/index.jsonl` untouched — a re-grade is a
    report, not runs; `git status` confirmed the only experiments/ change
    is the new directory.
  - Ran the script: **302 lanes → 58 KEEP / 238 KILL / 4 KILL-SIG /
    2 UNGRADEABLE**. KILL-SIG: GOOGL `adx_filtered_sma` t=−3.100 (r3-roc-adx),
    TSLA `rsi_mean_reversion` t=−3.011 (r3-meanrev-new-tickers), NVDA
    `cci_reversion` t=−2.930, AAPL `cci_reversion` t=−2.838 (both
    r3-aroon-cci); all daily, bar 2.638 at K=12. VERIFIED (not assumed)
    that SPY `donchian` (r3-trend-new-tickers, t=−2.417) stays plain KILL —
    the plan's near-miss prediction holds.
  - 18 new tests in `tests/test_promotion.py`: classify_verdict boundaries
    (t exactly −bar ⇒ KILL-SIG; −2.417 stays KILL; KEEP with t=−5 stays
    KEEP; None/NaN/non-numeric ⇒ KILL; mirrored bar rises with K) + script
    row-builder units on synthetic dicts + an integration pin re-grading
    the byte-committed TSLA rsi summary to KILL-SIG. No existing test
    touched.
  - `docs/research-round-4-results.md` NEW (badge `reference` in the first
    12 lines, round-3-results style) — results live here, NOT in the
    binding plan doc; R4-A section with counts table, the 4-lane KILL-SIG
    table, the UNGRADEABLE and 302-vs-~312 scope notes, and the explicit
    no-inversion statement. Reachability link added in
    `docs/current-state.md` next to the round-4 plan link.
- 2026-07-13T09:41Z — verify: `python3 -m pytest -q` → **455 passed**
  (437 on main + 18 new); pre-flip `bootstrap.py check --strict
  --require-session-log` red ONLY on the designed born-red gate.
- 2026-07-13T09:42Z — flip + claim-delete folded into this final commit.
  No merge action by this session; the auto-merge-enabler is the landing
  path.

## Previous-session review

⟲ The most recent prior card (`.sessions/2026-07-13-r4-design.md`, Status
`complete`) landed PR #99 clean — the Round-4 pre-registration this whole
session executes against. Its verify block reproduces at this session's
start: 437 passed matches its claimed count exactly, and
`docs/research-round-4-plan.md` sits at origin/main HEAD (`135101c`) with
badge `binding`, so R4-A's rule was committed before any Round-4 outcome
existed — the ordering its own 💡 wants machine-checked held here by
construction. Three observations from building on it: (1) its Close-out
"Next" sequencing (R4-A and R4-B first, cheapest, feeding R4-C) is what
this session followed — R4-A is now done at its predicted cost (0 configs,
0 backtests, one read-only pass); (2) its plan prose was verified rather
than trusted: the TSLA −3.01 / SPY −2.42 citations reproduced exactly from
the committed JSONs (−3.011, −2.417), but the "~312 existing summary
JSONs" estimate did NOT match the committed surface (302 files, 300
graded) — the plan hedged with "~" so nothing is wrong, and the results
doc records the reconciliation explicitly (this card's 💡 grew out of
that); (3) its hypothesis discipline pays off visibly: because R4-A
pre-registered "1–3 significantly-harmful lanes", the observed 4 is
reportable as a near-miss of a real prediction instead of a
post-hoc-flexible "small set". No defect found in its work.

## Close-out

**Done:** on branch `claude/r4-killsig` (PR #100) —

1. `trading_lab.promotion.classify_verdict` + `SWEEP_KEEP`/`SWEEP_KILL`/
   `SWEEP_KILL_SIG` — the pre-registered three-way verdict, total and pure;
   the ORDER 007 `grade_promotion` machinery untouched.
2. `scripts/run_r4_killsig_regrade.py` + its output
   `experiments/sweeps/r4-killsig-regrade/summary.json` — retroactive,
   NON-DESTRUCTIVE re-grade of all 302 committed r3 per-lane summaries:
   **58 KEEP / 238 KILL / 4 KILL-SIG / 2 UNGRADEABLE**; every r3 JSON
   byte-untouched, `experiments/index.jsonl` untouched.
3. `docs/research-round-4-results.md` (new running Round-4 results
   narrative, badge `reference`) with the R4-A section — headline counts,
   the 4 KILL-SIG lanes (GOOGL adx_filtered_sma −3.100, TSLA
   rsi_mean_reversion −3.011, NVDA cci_reversion −2.930, AAPL
   cci_reversion −2.838; bar 2.638 at K=12), SPY donchian −2.417 staying
   plain KILL, and the explicit statement that KILL-SIG is evidence
   AGAINST a lane and never an inversion signal — linked from
   `docs/current-state.md`.
4. 18 new tests (`tests/test_promotion.py`); no existing test weakened or
   deleted; the multiple-testing bar untouched (KILL-SIG only ever uses
   the lane's own recorded, already-Bonferroni'd bar, mirrored).
5. This card (born-red first commit `2befd2c` → implementation `117f380` →
   flipped `complete` last) — claim file deleted in this same commit.

**Verify:**
`python3 -m pytest -q` → 455 passed (437 on main + 18 new).
`python3 bootstrap.py check --strict --require-session-log --session-log .sessions/2026-07-13-r4-killsig.md`
→ green after this flip (pre-flip the only red was the designed born-red
gate). Integrity at close: diff touches only `src/trading_lab/promotion.py`
(additive), `scripts/run_r4_killsig_regrade.py` (new),
`experiments/sweeps/r4-killsig-regrade/` (new), `tests/test_promotion.py`
(additive), `docs/` (new results file + one link), `.sessions/`,
`control/claims/` (deletion). Every `experiments/sweeps/r3-*/` file
byte-untouched (non-destructive as pre-registered), holdout (SPENT)
untouched, `unlock_holdout` never passed, `data/p5holdout/` untouched,
`experiments/paper/**` byte-untouched, paper grading never run,
`control/inbox.md` + `control/status.md` + `control/outbox.md`
byte-untouched, no triggers created or modified, no broker/order/exchange
code, NO merge action taken by this session.

**Next (guard recipe):** none owed for correctness. Follow-ups now
unblocked: R4-B (cost-stress re-grade of the 64 KEEP-dev lanes) is the
next cheapest slice; future sweep runners can emit the three-way verdict
natively by calling `classify_verdict` at grade time (R4-F's plan already
anticipates "KILL-SIG at t ≤ −3.21 if R4-A has landed" — it has); the
tally-reconciliation check (this card's 💡) would catch prose/artifact
count drift like the ~312-vs-302 gap mechanically. Any OOS test of any
survivor remains OWNER-GATED on post-2026 data.

Session end: badge flipped `complete` in this final content commit before
push.
