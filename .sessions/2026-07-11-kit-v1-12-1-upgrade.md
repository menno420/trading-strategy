# 2026-07-11 — kit upgrade: substrate-kit v1.12.0 → v1.12.1

> **Status:** `complete`

📊 Model: fable-5 · kit-distribution seat (owner directive Q-0261.3) · start 2026-07-11

💡 **Session idea:** The regenerated substrate-gate now hard-reds any PR that
deletes a `.sessions/` card (append-only session memory, kit #226 G-2) — but
the claims convention *requires* deleting your own `control/claims/` file in
the flip commit. The two conventions are shape-identical ("delete a small md
file at close") yet opposite in polarity, and only the gate knows which is
which. Kit idea: have `check` emit a one-line "deletion polarity" hint when a
diff deletes any md under a convention-owned dir (`.sessions/` = never,
`control/claims/` = required-at-close) so a session doesn't have to hold both
rules from memory — the next footgun is an agent "fixing" a stale claim
deletion by restoring it, or pruning an old session card to tidy up.

## What happened

Vendored-kit upgrade v1.12.0 → v1.12.1 via the canonical release-asset path:
sha256-verified `bootstrap.py` asset
(`1055ca2cfd32a83e3dab7a978b05fbec2a82932a3375de0b1034f2519c16e4aa`, 704108
bytes) + adjacent `release.json`
(`cfce501f7798c15bbc86d33c67b91dfa1b46f21de4b43f14a456c8b8ae46b9a7`, 449
bytes, `"version": "1.12.1"`, breaking=false, no state migration), staged as
`bootstrap.py.new`, `python3 bootstrap.py.new upgrade` (staging inputs
self-cleaned). Payload: the substrate-gate **false-green fix** (kit review
#226) — sibling cards modified by a card-adding diff now gate through the
same `--require-session-log` locked door instead of advisory-only logging
(G-1), and session-card **deletions are a hard red** (session memory is
append-only; deletion-only diffs previously fell to the no-card advisory
path, G-2). Kit upgrade ONLY — no domain work; `control/inbox.md` /
`control/status.md` untouched.

## Work log

- Pre-upgrade tree at v1.12.0 (`substrate.config.json` `kit_version` L47; `bootstrap.py` `KIT_VERSION = "1.12.0"` L91), synced to origin/main `d3ae8f4`.
- Backups: exactly ONE new bank, `.substrate/backup/bootstrap-1.12.0.py`; pre-existing banks untouched (`bootstrap-1.1.0` … `bootstrap-1.11.0`, audit bank `substrate-gate.pre-regen-4f50eb4d.yml`).
- Carve-out scan — verbatim: `upgrade: carve-out scan: .github/workflows/substrate-gate.yml — ran, 0 found (live matched the previous kit template byte-for-byte; the delta is kit-side template evolution)` → gate regenerated in place, NO new pre-regen bank, no host jobs to relocate.
- Planted docs: 21 classified — 12 unchanged, 9 consumer-edited (template unchanged, nothing to apply), **0 diverged** — no `--apply-docs` rerun needed this wave (the v1.12.0 wave's AGENT_ORIENTATION divergence does not recur; that manual merge stays lane-owed).
- `.substrate/` staged copies refreshed (claude/CLAUDE.md, skills, agents, hooks, ci); still NO live root `CLAUDE.md` in this repo — staged copy remains invisible to workers until the lane adopts it.
- `check --strict` pre-flip → exit 1, only red the designed hold: "check: HOLD (by design): session card .sessions/2026-07-11-kit-v1-12-1-upgrade.md declares an in-progress Status …" (plus the same card's missing-markers advisory — same designed in-progress state, no other findings). Post-upgrade `bootstrap.py --version` → `substrate-kit 1.12.1`.
- Repo tests: `python3 -m pytest -q` → 223 passed.
- guard-fires: exactly ONE new `.substrate/guard-fires.jsonl` line — the designed born-red fire from the pre-flip `check --strict` (ts `2026-07-11T22:26:01+00:00`, this session's own card); committed in this flip commit per precedent.
- `control/inbox.md` + `control/status.md` untouched; claim file `control/claims/claude-kit-upgrade-v1.12.1.md` deleted IN this flip commit (the #56 lesson).

## Previous-session review

⟲ Previous-session review: the 2026-07-11 kit-v1-12-0-upgrade session (#61) again made this wave near-mechanical — its card carried the landing constraints (auto-merge OFF → MCP squash on green), the verbatim designed-HOLD line, and the claim-deleted-in-flip rule, and its new `(Nth wave)` aging convention for lane-owed follow-ups worked: the lane paid follow-up (1) (the `kit:` heartbeat bump) in PR #62, the very next session. What it could have done better: its close-out recorded the payload-head CI run ids but not the *flip*-head run ids — the runs that actually gated the merge — so this card records the flip-head runs explicitly. Workflow improvement: record the flip-head (merge-gating) run ids, not just intermediate heads; an audit of "what turned green to let this merge?" starts there.

## Close-out

- Follow-ups owed to the repo's own lane (NOT done here, per distribution-seat hard scope): (1) `control/status.md` `kit:` heartbeat line — now stale-by-one at v1.12.0 (the lane bumped it in #62; it goes stale again the moment this PR merges); (2) still no live root `CLAUDE.md` (kit copy staged at `.substrate/claude/CLAUDE.md`) — **4th wave**; (3) `docs/CAPABILITIES.md` landing-constraints entry still owed — **4th wave**; (4) `docs/AGENT_ORIENTATION.md` v1.12.0 manual merge (template delta preserved in the v1.12.0 upgrade-report) — **2nd wave**.
- Merge path: repo auto-merge toggle OFF → GitHub MCP `merge_pull_request` SQUASH on green, matching #51/#54/#55/#57/#60/#61 precedent.
- Next session should know: v1.12.1 in tree; the regenerated gate now hard-reds session-card deletions and locked-door-gates modified sibling cards — a close-out PR that touches another session's card will red until that card is also complete.
