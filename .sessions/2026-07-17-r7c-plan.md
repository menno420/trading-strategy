# 2026-07-17 — Round 7C pre-registration plan (drawdown × high-proximity conjunction)

> **Status:** `in-progress`

📊 Model: opus-4.8 · r7c-plan lane (overnight worker, owner order 2026-07-17T22:39Z / menu #01) · start 2026-07-18T00:18Z

💡 **Session idea:** once R7-C runs, stand up a small `docs/research-interaction-family-index.md` — a matrix mapping which component families have been CONJOINED (evaluated as an interaction) vs only graded in isolation. R7-C is the program's first registered conjunction (drawdown STATE × high-proximity STATE on two distinct windows); the index would make the next interaction obvious (e.g. drawdown × trend, proximity × volume) and stop the same pair being re-litigated as "new family vs variant" after outcomes. Anchor: the `Declared adjacency` blocks in `docs/research-round-{7,7c}-plan.md`; the index cell for each pair cites the plan/results doc that graded it. (Distinct from the scoreboard's cross-round rollup and the results-doc per-lane generator: those roll up rounds/lanes; this rolls up component-pair coverage.)

## Why this session exists

Menu #01 (overnight menu): pre-register the FIRST Round-7 interaction family —
the drawdown × high-proximity conjunction (`washout_recovery`), plan-before-outcome.
R7 graded `drawdown_reversion` (R7-A) and `high_proximity` (R7-B) only in
ISOLATION; menu #01 asks whether their CONJUNCTION on two distinct windows —
washed out from a long-horizon peak yet reclaiming a short-horizon high — carries
information the components lack. This session writes the BINDING pre-registration
(`docs/research-round-7c-plan.md`, badge `binding`): family, signal logic, the
12-variant grid, the registered null hypothesis, decision rule, and the exact
code + tests a future run session must add BEFORE any run. PLAN ONLY — this
session runs NOTHING, commits no grid to `sweeps.py`, and touches no strategy /
backtest code; executing R7-C is a future session's separately claimed slice.
Build-then-review: implement, open PR READY, leave the card `in-progress` for the
owner's correctness check before flip.

## Work log

- branch `claude/research-round-7c-plan` cut from origin/main HEAD (R7 run PR #141
  + program scoreboard PR #142 — program cumulative 5,415). Born-red FIRST commit:
  this card.

## Previous-session review

[[fill:]]

## Close-out

[[fill:]]
