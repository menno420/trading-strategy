# 2026-07-17 — Round 7C pre-registration plan (drawdown × high-proximity conjunction)

> **Status:** `complete` — Round 7C pre-registered (PLAN ONLY) —
> docs/research-round-7c-plan.md (badge binding): washout_recovery drawdown ×
> high-proximity conjunction, 12-variant grid (2×3×2) → 180 configs, program
> 5,415 → 5,595; running is a future slice; dev-only, promotion CLOSED, no
> code committed to sweeps.

📊 Model: opus-4.8 · r7c-plan lane (overnight worker, owner order 2026-07-17T22:39Z / menu #01) · start 2026-07-18T00:18Z

💡 **Session idea:** once R7-C runs, stand up a small `docs/research-interaction-family-index.md` — a matrix mapping which component families have been CONJOINED (evaluated as an interaction) vs only graded in isolation. R7-C is the program's first registered conjunction (drawdown STATE × high-proximity STATE on two distinct windows); the index would make the next interaction obvious (e.g. drawdown × trend, proximity × volume) and stop the same pair being re-litigated as "new family vs variant" after outcomes. Anchor: the `Declared adjacency` blocks in `docs/research-round-{7,7c}-plan.md`; the index cell for each pair cites the plan/results doc that graded it. (Distinct from the scoreboard's cross-round rollup and the results-doc per-lane generator: those roll up rounds/lanes; this rolls up component-pair coverage.)

## Why this session exists

Menu #01 (overnight menu): pre-register the FIRST Round-7 interaction family —
the drawdown × high-proximity conjunction (`washout_recovery`), plan-before-outcome.
R7 graded `drawdown_reversion` (R7-A) and `high_proximity` (R7-B) only in
ISOLATION; menu #01 asks whether their CONJUNCTION on two distinct windows —
washed out from a long-horizon peak yet reclaiming a short-horizon high — carries
information the components lack. This session writes the BINDING pre-registration
(`docs/research-round-7c-plan.md`, badge `binding`): family, signal logic, the
12-variant grid, the registered null hypothesis, decision rule, and the exact
code + tests a future run session must add BEFORE any run. PLAN ONLY — this
session runs NOTHING, commits no grid to `sweeps.py`, and touches no strategy /
backtest code; executing R7-C is a future session's separately claimed slice.
Build-then-review: implement, open PR READY, leave the card `in-progress` for the
owner's correctness check before flip.

## Work log

- branch `claude/research-round-7c-plan` cut from origin/main HEAD (R7 run PR #141
  + program scoreboard PR #142 — program cumulative 5,415). Born-red FIRST commit:
  this card.

## Previous-session review

⟲ PR #143 (`strategy-catalog`, merged) — landed `docs/strategy-catalog.md`,
one reference table for all 32 strategy families (class · one-line thesis ·
round tested · standing dev-only verdict · per-row source), linked from
`docs/repo-navigation-map.md`; docs-only, no verdict changed, promotion CLOSED
/ 0 promoted. **Adjacency reconciliation with this plan:** the catalog carries
exactly two Round-7 family rows — `drawdown_reversion` §R7-A and
`high_proximity` §R7-B — and those are precisely the two nearest burned
neighbors this plan declares R7-C conjoins (`docs/research-round-7c-plan.md`
Declared-adjacency block: R7-A pure long-window drawdown STATE with hysteresis
exit, no proximity; R7-B pure single-window proximity STATE, no drawdown; R7-C
the conjunction of a long-horizon washout AND a short-horizon recovery on two
distinct windows, reducible to neither). The catalog was in fact built as the
"cite, don't re-derive" adjacency reference this pre-registration leans on, so
the two documents agree on the neighbor set and on the both-dev-only standing
of each component — the plan's adjacency claim is corroborated, not asserted.
**One honest nit (not a defect):** the catalog states each family's *standing*
verdict but does not itself carry the per-window structural parameters (the
252-bar peak window / ≤63-bar high) that make R7-C non-reducible to its
neighbors; that structural difference lives only in the plan doc, so a reader
relying on the catalog alone sees the two families are adjacent but not *why*
their conjunction is a distinct decision surface. That is by design (the
catalog is a family index, not a signal spec) — no defect found in the R7
rows this plan depends on.

## Close-out

**Done** (3 commits):
- `bd20294` — born-red FIRST commit: this session card (`in-progress` hold).
- `c27035d` — `docs/research-round-7c-plan.md` (badge `binding`: family,
  signal logic, 12-variant grid, registered null, decision rule, required
  code + tests) + the `docs/current-state.md` current-state pointer.
- (this commit) — close-out flip: badge `in-progress` → `complete`,
  previous-session-review + close-out slots resolved.

**Verify:**
- `python3 -m pytest -q` → 694 passed.
- `python3 bootstrap.py check --strict` → EXIT 0 at flip (the born-red
  in-progress hold clears when the badge flips to `complete`).
- Integrity: the branch diff touches only `docs/research-round-7c-plan.md` +
  the `docs/current-state.md` pointer + this session card. PLAN ONLY — no
  code / grid / run / verdict / holdout / paper / control-status / trigger
  change; no grid committed to `sweeps.py`, no strategy or backtest code,
  `min_tstat` bar unchanged, promotion CLOSED / 0 promoted, holdout untouched
  (SPENT). NO manual merge — the auto-merge enabler lands PR #144 on green.

**Next:** the R7-C RUN is the next separately-claimed slice — it adds
`src/trading_lab/strategies/washout_recovery.py`,
`sweeps._R7C_CONJUNCTION_AXES` with `r7c_conjunction_variants` /
`r7c_total_configs`, and the pinning & causality tests in the FIRST code
commit (grid + counts pinned before any run), then runs
`scripts/run_r7c_*.py` and lands `docs/research-round-7c-results.md` — exactly
the R7 run mechanics, plan-before-outcome honored.
