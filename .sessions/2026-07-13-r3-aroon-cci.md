# 2026-07-13 — R3 slice 4: Aroon trend + CCI reversion sweep (dev-only)

> **Status:** `complete` — two new classic-indicator families built, tested,
> and swept over a 12-ticker mixed set (frozen 8-ticker universe +
> SPY/QQQ/TSLA/TLT lane-local) on the standard dev rail; honest outcome
> **0 PROMOTED / 4 KEEP (dev-candidate only) / 20 KILL** of 24 lanes. This
> card was born red (`in-progress`) and flipped `complete` as the
> deliberate last content change before push; the claim file is deleted in
> this same commit.

📊 Model: fable-5 · r3-aroon-cci lane (worker session) · start 2026-07-13T01:33Z

💡 **Session idea:** per-lane verdicts have no queryable home — every
KEEP/KILL lives only inside its own sweep JSON (100+ files across
`experiments/sweeps/*/`) and in PR-body prose, so building this PR's
results table meant re-parsing script stdout, and the Friday grading has
no scoreboard it can diff week over week. The pattern this lane just
re-confirmed (KEEPs cluster where the benchmark itself is weak — SLV/TLT
here, XOM/TLT in slice 3, META in slices 1-2) is exactly the kind of
cross-lane question nobody can currently ask without ad-hoc glob+jq.
Adopt a verdicts index: teach the sweep-write path to also append one
compact row per lane (sweep, family, instrument, oos_sharpe,
bench_sharpe, tstat, min_tstat, verdict, data_end) to
`experiments/sweeps/index.jsonl`, rebuilt from the summary JSONs the same
way `ledger.rebuild_index()` rebuilds `experiments/index.jsonl` (anchor:
the `sweep_record` write loop shared by every `scripts/run_r3_*_sweep.py`;
natural home if slice 1's sweep-runner extraction lands: the shared
runner emits the row for free; test target: a `test_ledger.py`-style
round-trip check that the rebuilt index matches the JSONs on disk).
Recent cards' ideas (sweep-runner extraction, filter-off control arms —
implemented by this lane's committed aroon control arm, cache-provenance
sidecar, docs-gate advisory, in-flight decay advisory) don't cover this.

## Why this session exists

ORDER 012 (control/inbox.md, owner night-run direct order, 2026-07-13)
instructs this seat: "TRADING RESEARCH: expand the backtest surface — new
strategies, new stocks/tickers, new indicators — every result recorded
honestly." Slices 1-3 (PRs #81, #82, #83) covered oscillator reversion,
trend/momentum, and six new instruments; this slice adds the two most-cited
classic indicators the library still lacks — the Aroon oscillator
(`aroon_trend`, trend-following) and Lambert's Commodity Channel Index
(`cci_reversion`, mean-reversion) — and is the first lane to sweep a MIXED
instrument set: the full frozen 8-ticker universe PLUS four of slice 3's
new instruments (SPY, QQQ, TSLA, TLT), exercising the new caches on new
strategy families. Both strategies slot into the existing long/flat
conventions (signal at bar t, engine fills at bar t+1 open) with modest
pre-declared grids; per slice 2's card idea, the classic zero-band arm
(`entry == exit == 0`, the pure Aroon-Up/Aroon-Down cross) is committed IN
the aroon_trend grid as the within-family control. Promotion is CLOSED
post-holdout: mostly-KILL was the expected outcome and is recorded as a
first-class result.

## Work log

- 2026-07-13T01:33Z — clone hard-synced to origin/main HEAD `dbb5396`
  (verified byte-identical to `git ls-remote origin main`).
  `control/inbox.md` read at HEAD (read-only): newest order is **ORDER 012 ·
  2026-07-13T00:46:37Z · status: new** (owner night-run direct order) — this
  session executes its "TRADING RESEARCH: expand the backtest surface" lane
  (slice 4). Collision check: `control/claims/` at HEAD contains only
  `README.md` + `2026-07-13-order-night-run.md` — no overlap with this
  scope. Branch `claude/r3-aroon-cci`; this card + the claim
  `control/claims/2026-07-13-r3-aroon-cci.md` are the born-red FIRST
  commit (`84d356d`), pushed before any build work.
- 2026-07-13T01:37Z — commit `98bea07`: strategies `aroon_trend`
  (Aroon-Up/Down = 100·(period − bars-since-extreme)/period over a trailing
  period+1 window, standard Chande construction; long on oscillator >
  `entry` strict, exit on oscillator < `exit` strict, held in the
  `exit <= entry` hysteresis band between; `entry == exit == 0` recovers
  the classic Aroon cross and is the committed within-family control arm —
  implementing slice 2's card convention; warm-up flat, all-tie flat
  prices resolve to oscillator 0 = no signal) and `cci_reversion` (Lambert
  CCI = (TP − SMA(TP, period)) / (0.015 · MAD) over typical price; long on
  CCI crossing UP through `buy_below` from oversold — recovery
  confirmation, same convention as the stochastic/Williams %R families —
  exit on CCI > `sell_above`, exit wins same-bar ties; zero-MAD undefined
  = flat); registered in `STRATEGIES`/`DEFAULT_PARAMS` +
  `R3_AROON_CCI_FAMILY`; grids declared in `trading_lab.sweeps` BEFORE the
  sweep ran (12 variants/family: aroon period {14,25,50} × entry {0,50} ×
  exit {−50,0} with exit ≤ entry; cci period {14,20,28} × buy_below
  {−150,−100} × sell_above {0,100}); instruments = frozen universe + 4
  slice-3 caches, lane-local, `config.UNIVERSE` untouched; 13 new unit
  tests + 8 grid tests (incl. a committed-control-arm check), all offline
  synthetic data.
- 2026-07-13T01:40Z — sweep run (`scripts/run_r3_aroon_cci_sweep.py`,
  mirrors the slice-1/2 scripts: `load_ohlcv` default rail, dev bars end
  **2025-01-08** on every lane, costs 5 bps + 1 bp per side, walk-forward
  1008/252 contiguous stitched OOS vs same-window same-cost B&H; 288
  registered configs, program cumulative 1460 = 1172 prior + 288).
  Honest verdicts (Round-2 KEEP/KILL rule; ORDER 007 t-stat at Bonferroni
  K=12 → min t 2.64 recorded per lane, informational only, promotion
  CLOSED):
  - aroon_trend: **9/12 KILL**; KEEPs (dev-candidate only) AAPL (OOS 1.132
    vs 0.963, t=0.53), META (0.880 vs 0.653, t=0.64), TLT (0.335 vs 0.196,
    t=0.44).
  - cci_reversion: **11/12 KILL**; SLV KEEP (0.297 vs 0.170, t=0.40) —
    the weakest reversion family tested so far: 0 KEEPs on the universe's
    uptrend names, worst lanes NVDA (t=−2.93) and AAPL (t=−2.84).
  - Honest pattern: the KEEPs again cluster where the benchmark itself is
    weak (SLV/TLT B&H OOS Sharpe ≤ 0.2; META the family-agnostic repeat
    offender) — a low bar cleared, not an edge. All four KEEPs deep inside
    noise vs the 2.64 bar; NOT findings, nothing promoted. 24 sweep
    summaries in `experiments/sweeps/r3-aroon-cci/`, 24 ledger rows (one
    top-variant run per lane, `variants_tried=12`), index rebuilt via
    `trading_lab.ledger.rebuild_index()`. Commit `c89da7a`.
- 2026-07-13T01:42Z — PR **#84** opened READY (non-draft) with the full
  honest results table; no merge action by this session (auto-merge-enabler
  is the landing path).

## Previous-session review

⟲ The most recent prior card (`.sessions/2026-07-13-r3-new-tickers.md`,
Status `complete`) landed PR #83 clean and is half the template this slice
mirrored: born-red first commit, lane-local instruments with the frozen
universe untouched, dev-rail sweep with honest 5 KEEP-dev / 13 KILL
verdicts, flip+claim-delete folded into one final commit — correct
discipline throughout, and its verify block's claimed counts (279 passed,
bootstrap exit 0) reproduce at this session's start. Its six caches are
real and immediately useful: this session ran two brand-new families over
four of them (SPY/QQQ/TSLA/TLT) with zero data issues — the first
realization of its close-out's transfer-note that the new instruments make
cross-instrument checks cheap. Its 💡 (cache-provenance sidecar) is sound
and still unimplemented — this session again could not have proven WHICH
source produced the caches it consumed; endorsed. Its endorsement stack is
getting tall though (sweep-runner extraction now owed an eighth copy by
this session; filter-off control arms; provenance sidecar) — of these,
this session directly implemented the control-arm convention for its own
aroon grid, so that idea now has a second concrete instance in tree.

## Close-out

**Done:** on branch `claude/r3-aroon-cci` (PR #84) —

1. Two new strategies + registration
   (`src/trading_lab/strategies/aroon_trend.py`, `cci_reversion.py`,
   `__init__.py`).
2. Pre-declared Round-3 slice-4 grids over the 12-ticker mixed set
   (`src/trading_lab/sweeps.py`) + tests (`tests/test_strategies.py`,
   `tests/test_sweeps.py`, incl. the committed aroon control-arm check).
3. Sweep script `scripts/run_r3_aroon_cci_sweep.py` + 24 lane summaries
   under `experiments/sweeps/r3-aroon-cci/` + 24 ledger rows + rebuilt
   `experiments/index.jsonl`.
4. This card (born-red first commit → flipped `complete` last) — claim
   file deleted in this same commit.

**Verify:**
`python3 -m pytest -q` → 303 passed.
`python3 bootstrap.py check --strict --require-session-log --session-log .sessions/2026-07-13-r3-aroon-cci.md`
→ exit 0 after this flip. Integrity at close: dev rail only (every lane
`data_end = 2025-01-08`), holdout (SPENT) untouched, `unlock_holdout`
never passed, paper-lane files byte-untouched, `control/inbox.md` +
`control/status.md` byte-untouched, `config.UNIVERSE` untouched (extra
instruments lane-local), no broker/order/exchange code, no
promotion/finding language beyond the recorded KILL/KEEP-dev verdicts, NO
merge action taken by this session.

**Next (guard recipe):** none owed for correctness. Follow-up candidates:
the sweep-runner extraction (slice 1's 💡, endorsed again — eighth copy),
the cache-provenance sidecar (slice 3's 💡), and the sweep-verdicts index
(this card's 💡 — anchors and test target named there). The four KEEPs
are dev-candidates only; any OOS test of them is OWNER-GATED behind a new
pre-registered protocol on post-2026 data, per
docs/research-round-2-results.md.

Session end: badge flipped `complete` in this final content commit before
push.
