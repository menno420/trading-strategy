# 2026-07-13 — Round 6 plan: pre-registered surface expansion under the selection-fair gate

> **Status:** `complete` — ORDER 014 items 1+3 delivered in one PR,
> plan-before-outcome: `docs/research-round-6-plan.md` (badge `binding`,
> committed before any Round-6 outcome exists) pre-registers three slices
> on the COMMITTED caches only — R6-A volume families (`obv_trend` +
> `mfi_reversion`, the program's first volume-based indicator class) × 15
> daily tickers, R6-B `overnight_gap` (the first open-column signal
> family, fade/follow mirror theses in one grid) × 12 equity/ETF tickers,
> R6-C the volume families × 8 hourly tickers — 696 new registered
> configs (program cumulative 4359 → 5055), 58 lanes, per-lane K=12, bar
> unchanged. Grids + three new strategy families + tests landed in the
> SAME PR before any sweep runs; NO sweep executed here (execution is the
> separately claimed `claude/round-6-run` slice). The R5-D
> fixed-config-row convention and the selection-fair gate ([D-0002]) are
> folded in as standing rules. This card was born red and flips
> `complete` in this final commit; the round-6-plan claim file is deleted
> in this same commit.

📊 Model: fable-5 · round-6-plan lane (coordinator-dispatched night worker, ORDER 014 items 1+3) · start 2026-07-13T22:36Z

💡 **Session idea (deduped against recent cards — distinct from the
round-5 card's standing-gate idea, the selection-fair-gate card's
`reason_class` taxonomy, the r4-regime card's cardinality-matched control
arm, and the kill-sig-review card's verdict-grammar unification):** the
program's multiple-testing burden (4,359 → 5,055 registered configs) is
hand-carried PROSE — every plan/results doc restates the cumulative and a
reader must diff documents to audit it. Make the burden ledger
machine-readable and drift-proof: a committed `experiments/burden.json`
(one row per grid commit: slice id, new configs, cumulative) that every
grid registration updates, plus a pin in `tests/test_sweeps.py` asserting
the sum of all registered `*_total_configs()` equals the counter's
cumulative — a plan doc that claims a different number then fails CI
instead of drifting silently. Anchors: `sweeps.r6_total_configs` (the
first round-level totaler, added this session — the natural hook), test
target: a burden-reconciliation fixture in `tests/test_sweeps.py`.

## Why this session exists

ORDER 014 (EAP final-night worklist, `control/inbox.md` @ `c9297a7`) items
1 and 3: write and commit a pre-registered Round-6 plan — new idea classes
/ tickers / indicators under the selection-fair standing gate (ORDER 012
item 4 standing direction; gate PR #111 `d498018`, decision [D-0002]) — and
fold the R5-D convention (a fixed-config row in any searched-arm
comparison, `docs/research-round-5-results.md` closing tally item 4) into
that plan as a standing rule. Plan-before-outcome: this PR lands the plan,
the pre-declared grids in `trading_lab.sweeps`, and their tests BEFORE any
Round-6 sweep runs; execution is a separate claimed slice
(`claude/round-6-run`, ORDER 014 item 2).

## Work log

- 2026-07-13T22:36Z — branch `claude/round-6-plan` cut from origin/main
  HEAD `c9297a7`. Collision check: `control/claims/` at HEAD contains only
  the README — no live claims; the round-6 claims land via the ack PR
  (#114, branch `claude/order-014-ack`). Born-red FIRST commit: this card
  (`in-progress`).
- 2026-07-13T22:37Z — ack PR #114 (control fast lane: ORDER 014 ack under
  the inbox thread + the two round-6 claim files) merged by the
  auto-merge enabler at 22:37:17Z (`github-actions[bot]`), main →
  `d929972`.
- 2026-07-13T22:40Z — data-first design pass BEFORE registering anything:
  inspected `data/` — 15 daily caches (volume verified non-degenerate: 0
  zero-volume bars), 8 hourly caches (≤ 10 zero-volume bars each,
  recorded in the plan), `data/p2ext/` only 2 tickers (recorded in the
  plan as considered-and-NOT-registered — too thin), `data/p5holdout/`
  NEVER read. Conclusion registered honestly: no new tickers exist
  without a fetch; the new idea classes are the unused volume column and
  the unused open-column signal territory.
- 2026-07-13T22:44Z — pre-declaration commit `18f83ac` (grids-before-run
  rail): three new strategy families (`obv_trend`, `mfi_reversion`,
  `overnight_gap` — each with warm-up/causality semantics documented and
  the prefix-invariance contract satisfied), `_R6_VOLUME_AXES` /
  `_R6_GAP_AXES` + hourly reuse in `trading_lab.sweeps` (12
  constraint-valid variants per family; totals 360/144/192 = 696), and
  the test pins: registry membership, counts, K=12 with min_tstat(12) ≈
  2.64 never lowered, verbatim instrument-tuple identity (R4-F 15-ticker,
  slice-8 12-ticker with BTC-USD excluded, r3-trend-hourly 8-ticker),
  both gap theses committed in one grid, the pure-OBV control arm pinned
  per window, hourly grid identity with R6-A, and every-variant-runs.
- 2026-07-13T22:45Z — plan commit `7e5c847`:
  `docs/research-round-6-plan.md` (badge `binding` within the first 12
  lines, R4/R5 structure) + reachability link in `docs/current-state.md`.
  Standing rules folded in: (1) selection-fair gate on every KEEP —
  `run_selection_gate`/`apply_gate` with the fidelity guard, gate block
  required in every lane JSON ([D-0002], PR #111 `d498018`); (2) R5-D
  fixed-config row in every searched-arm comparison (ORDER 014 item 3) —
  the gate runs on EVERY lane so KILLed lanes carry the row too. Rails
  restated: dev rail `load_ohlcv` only, `min_tstat(K)` never lowered,
  promotion CLOSED, costs baseline, per-slice runtime caps (20/15/20 min)
  with an explicit no-silent-truncation rule.
- 2026-07-13T22:47Z — heartbeat commit `82f8262` (`control/status.md`
  night-progress lines for items 1/2/3/4+5), then merged origin/main
  (`f9549f7`) to bring the #114 claim files onto this branch for the
  close-out deletion. Verify + close-out: this commit.

## Previous-session review

⟲ PR #112 (`control: heartbeat + outbox 2026-07-13T16`, squash `499876f`)
— the coordinator-dictated heartbeat this session inherited. Checked
against the tree and live GitHub state: its payload matches its promise
(control-only diff), its `main:` line `d498018` is exactly PR #111's real
squash SHA (verified via `git log`), its `grading_executor` line names
the LIVE trigger and next-fire the ORDER 014 worklist item 4 now asks to
pre-verify, and its `round_6: ready behind the new selection-fair gate,
awaiting direction` line is precisely the state ORDER 014 then consumed —
a heartbeat that made the next session's dispatch decision trivially
checkable. No defect found in its work.

⟲ PR #111 (selection-fair standing gate, `d498018`, card
`.sessions/2026-07-13-selection-fair-gate.md`) — this plan binds every
Round-6 runner to its contract, so its claims were re-verified at source
this session: `src/trading_lab/selection_gate.py` matches the card —
`apply_gate` only ever demotes (KEEP → KILL on FAIL, non-KEEP passes
through), every UNGRADEABLE condition FAILs with a verbatim reason
rather than skipping, and the `> 0` conjunct strictness increase is
present in `gate_decision` as documented. Its card's follow-up item 2
("the first round-6 runner must actually CALL the gate; a runner that
mints KEEP-dev without a `selection_gate` block violates [D-0002]") is
now a REGISTERED standing rule of the Round-6 plan rather than a card
note — the correct escalation of a guard recipe into binding text. No
defect found in its work.

## Close-out

**Done:** on branch `claude/round-6-plan`, six commits `dace379` (born-red
card) → `18f83ac` (pre-declaration: 3 strategy families + R6 grids + 39
new tests) → `7e5c847` (binding plan doc + reachability link) → `82f8262`
(heartbeat) → `f9549f7` (merge of main for the claim lifecycle) → this
close-out (card flipped `complete`, claim
`control/claims/2026-07-13-round-6-plan.md` deleted; the
`claude/round-6-run` claim deliberately LEFT in place — that slice is
still unexecuted and belongs to the run session).

1. `docs/research-round-6-plan.md` (badge `binding`): three slices with
   hypotheses, committed grids, decision rules, burden, runtime caps and
   the no-silent-truncation rule; two standing rules (gate + R5-D row);
   honest nulls expected and said so; considered-and-rejected directions
   recorded.
2. `trading_lab.sweeps`: `r6_volume_*`, `r6_gap_*`, `r6_volume_hourly_*`,
   `r6_total_configs` — 696 new registered configs, cumulative 4359 →
   5055, every count and tuple identity pinned by tests.
3. Three new strategy families with dedicated tests plus the registry's
   generic validity/causality parametrization now covering 28 families.
4. NO sweep run, NO experiments/ artifact touched, NO results doc
   created — the plan precedes every outcome by construction.

**Verify:** `python3 -m pytest -q` → **646 passed** (607 baseline + 39
new; no existing test touched). `python3 bootstrap.py check --strict` →
green with this card complete (pre-flip it held two reds: the designed
born-red hold, and a kit `[stamp]` finding — the plan initially cited the
gate decision's `D-NNNN` id from two more docs, and the kit's stamp
discipline wants each decision id cited from exactly ONE home doc; fixed
by citing the home doc `selection-fair-gate.md` instead of the raw id). Integrity at close: diff vs main touches only the plan
doc, the `docs/current-state.md` link, `src/trading_lab/sweeps.py` +
`tests/test_sweeps.py`, the three new strategy modules +
`strategies/__init__.py` + `tests/test_strategies.py`, `control/status.md`
(heartbeat), `.sessions/`, and the claim deletion. Every `experiments/**`
file byte-untouched; `data/p5holdout/` never read, `unlock_holdout` never
passed; `control/inbox.md`/`outbox.md` byte-untouched by this branch
(the ack landed via PR #114); no triggers created or modified; no
broker/order/exchange code; R5-C BTC OOS not scheduled; MTF-Bollinger
prereg untouched (FROZEN); NO merge action by this session on its own
PRs — the auto-merge enabler is the landing path.

**Next (guard recipe):** the run session (`claude/round-6-run`) must (1)
call `selection_gate.run_selection_gate(...,
recorded_searched_sharpe=<lane's own searched Sharpe>)` and `apply_gate`
in every R6 runner before writing any verdict — anchor:
`docs/selection-fair-gate.md` call sketch; a KEEP-dev without a
`selection_gate` block in the lane JSON violates [D-0002]; (2) honor the
per-slice caps (1200 s / 900 s / 1200 s) with the plan's CAP-HIT
protocol, never truncate silently; (3) delete
`control/claims/2026-07-13-round-6-run.md` in its own close-out commit;
(4) inherit the recorded hourly zero-volume-bars fact (≤ 10/ticker) —
`mfi_reversion` already treats zero flow as flat, so no code change is
owed, but the results doc should note it if any hourly lane grades
UNGRADEABLE.

Session end: badge flipped `complete` in this final content commit
before push.
