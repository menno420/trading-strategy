# 2026-07-09 — P0 build: data layer, engine, baselines, ledger (ORDER 001, Phase B)

> **Status:** `in-progress`

📊 Model: claude-agent · high · feature build

💡 **Session idea:** Build roadmap P0 end-to-end: cached daily+hourly data layer with the locked-holdout rail enforced in code, a compact vectorized backtest engine (t+1 open execution, costs on by default), three baseline strategies, the one-file-per-run experiment ledger, walk-forward helper, offline unit tests, pytest CI — then seed the ledger with the first real baseline runs.

## Previous-session review

Previous session (2026-07-09-adopt-substrate-kit, Phase A) adopted substrate-kit v1.1.0: docs planted and rendered, CI gate at .github/workflows/substrate-gate.yml, session loop engaged; `check --strict` exits 0. Its guard recipe for this session: create src/, tests/, data/, requirements.txt and a pytest CI workflow; add ownership entries for the new areas; keep new docs reachable from docs/AGENT_ORIENTATION.md. All followed here.

## Work log

- `requirements.txt` (pinned: pandas 3.0.3, numpy 2.4.6, yfinance 1.5.1, requests 2.33.1, pytest 9.1.1) + `pyproject.toml` (pytest pythonpath).
- `src/trading_lab/`: config (universe, `HOLDOUT_START=2025-01-09`, cost defaults), data (yfinance → Yahoo chart API → stooq fallbacks; csv.gz cache; holdout enforced at load with warn-loudly `unlock_holdout` escape hatch; integrity checks), engine (custom vectorized backtester, t+1-open fills, per-side bps costs), metrics (timeframe-aware annualization), strategies (buy_and_hold, sma_crossover, rsi_mean_reversion), ledger (one JSON per run + regenerated index.jsonl), walkforward (rolling train/test splits + grid fit).
- Decide-and-flag: custom ~150-line vectorized engine over vectorbt/backtesting.py (P1 needs sweep-scale vectorization + full control of cost model and t+1 fills, fully unit-testable, no heavy pin); GLD/SLV ETFs over GC=F/SI=F futures (cleaner free hourly data, no roll artifacts); index.jsonl regenerated-not-appended (idempotent, conflict-free).
- Data cache committed: 8 tickers × daily (2010-01-04→now) + hourly (~2.9y — Yahoo gave more than the documented 730d), ~1.6 MB total. Quirks documented in docs/p0-lab-guide.md (partial last bar, META IPO 2012, half-day hourly sessions, NaN hourly rows, proxy-vs-curl_cffi TLS note).
- Tests: 63 offline tests — no-lookahead proof, cost application, holdout lock, strategy validity + causality, hand-computed metrics, ledger schema, walk-forward boundaries. CI: .github/workflows/tests.yml (Python 3.11, pip install -r requirements.txt, pytest -q).
- Docs: docs/p0-lab-guide.md (execution model, holdout policy, how-to, quirks), linked from AGENT_ORIENTATION.md and current-state.md; ownership rows for src/, tests/, data/, experiments/.
- Seeded the ledger: 24 baseline runs (3 strategies × 8 instruments, daily, default params, pre-holdout dev data, variants_tried=1 each) + regenerated index.

## Close-out

(to be completed before final commit)
