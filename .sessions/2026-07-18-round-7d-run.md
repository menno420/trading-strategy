# 2026-07-18 — Round 7D run: execute the pre-registered xsec_drawdown cross-sectional slice

> **Status:** `complete` — Round 7D executed — xsec_drawdown cross-sectional
> drawdown ranking on XSEC-14: 0 KEEP / 6 KILL / 0 KILL-SIG of 6 lanes, 0
> promoted, best informational t −0.74 (L63/k3) vs the K=6 bar ≈2.39; all 6
> lanes below the 1.193 equal-weight basket B&H, none significantly worse
> (uninformative, not value-destroying); selection-fair gate run per lane (6
> FAIL_UNDERPERFORM, 0 UNGRADEABLE, degenerate for 1-config portfolio lanes:
> fixed==searched); runtime 2.1 s vs 900 s cap; program 5,595 → 5,601.
> Results: docs/research-round-7d-results.md.

📊 Model: opus-4.8 · round-7d-run lane (overnight worker, owner GO 2026-07-18T13:47Z) · start 2026-07-18T15:28Z

## Why this session exists

Owner GO 2026-07-18T13:47Z authorizes executing the pre-registered
Round-7D plan (`docs/research-round-7d-plan.md`, badge `binding`, merged as
PR #147, PLAN-ONLY until now). Round 7D registers ONE genuinely new family
on the committed daily caches only — `xsec_drawdown`, the first
cross-sectional PORTFOLIO family that ranks the XSEC-14 basket on DRAWDOWN
DEPTH (depth from a trailing L-bar close peak) rather than on trailing
point-to-point RETURN the way `xsec_momentum` (long winners) and
`xsec_reversal` (long losers) do. The question: does drawdown STATE carry
cross-sectional information where the two return-ranked neighbors and the
single-name drawdown lanes (R7-A) mostly nulled?

This session adds the family + grid + pinning/causality tests in the first
code commit (plan-before-outcome), then runs the 6-lane portfolio slice
under the Round-2 §6 KEEP/KILL rule (stitched portfolio-walk-forward OOS
Sharpe > equal-weight basket B&H OOS Sharpe AND > 0) on every lane, and
writes `docs/research-round-7d-results.md`. POST-HOLDOUT, DEV-ONLY: the
holdout is SPENT and promotion is CLOSED — a KEEP anywhere is a
dev-candidate only, never a finding. The `min_tstat` bar (≈2.39 at the xsec
K=6 convention) is UNCHANGED and never lowered.

## Work log

- branch `claude/round-7d-run` cut from origin/main HEAD (incl. #147, the
  R7D pre-registration). Born-red FIRST commit: this card.

💡 **Session idea:** the four cross-sectional PORTFOLIO ranking families
now in the library (`xsec_momentum` long top-k by trailing return,
`xsec_reversal` long bottom-k by trailing return, and R7-D `xsec_drawdown`
long deepest-k by drawdown depth) share one verbatim skeleton — validate
params, build an all-NaN weight frame, loop the anchored rebalance
schedule, `np.lexsort((tie_break, key_row))`, set `1/k` on the first k of
the order. Only the per-bar RANKING KEY differs (`close/close.shift(N)−1`
vs `close/close.rolling(L).max()−1`). Promote a single
`rank_and_weight(closes, key_frame, k, rebalance_every, ascending) ->
DataFrame` helper so a new cross-sectional family registers ONLY its
key-frame expression and inherits the tested schedule/tie-break/sum-to-1
mechanics; anchor: the identical `for t in range(START, len(closes),
rebalance_every)` block copied across all three modules; test target: a
golden test that the helper reproduces each committed family's weights
bit-for-bit. (Distinct from the R7C grid-expander idea, which dedupes the
`sweeps.py` axes-plus-`product` generator — this dedupes the STRATEGY-side
ranking loop.)

## Previous-session review

⟲ PR #147 (`r7d-plan`, merged) — pre-registered the Round-7D `xsec_drawdown`
cross-sectional slice (badge `binding`, PLAN ONLY): the family + signal logic
(`close/rolling(L).max()-1`), the 6-variant grid (`L`{63,126,252} × `k`{2,3},
rebalance frozen 21), the registered null, the portfolio KEEP/KILL rule, and
the exact code + tests the run must add first. **Did this run honor the
pre-registration?** Yes, on every registered axis. The 6 configs ran exactly
as pinned — `L`{63,126,252} × `k`{2,3}, portfolio config-count = 6, rebalance
frozen at 21 — on the committed XSEC-14 basket, and the family + grid + pinning
& causality tests landed in the first code commit (`a79de47`) before any run
outcome existed. The registered baseline null ("most/all lanes KILL") **HELD at
6/6 KILL** — drawdown STATE carried no cross-sectional edge where the
return-ranked neighbors and single-name drawdown lanes nulled. The plan's
registered concentration mirror — the prediction that k=2 / longer-L would
out-grade if any gradient existed — **ran the wrong way** (no monotone gradient
in L or k cleared the bar), which is exactly the "no gradient clears"
degenerate the plan anticipated for a null family. The R7-C-cautioned *harm*
tail (significantly-worse-than-B&H KILL-SIG) did **not** materialize: all 6
lanes underperform the 1.193 B&H but none significantly (0 KILL-SIG) —
uninformative, not value-destroying. **One honest nit worth recording (not a
defect):** the plan's standing rule 1 (run the selection-fair gate on *every*
lane) was initially missed by the R3-cloned xsec runner and had to be added in
a follow-up commit (`9e9c1dc`) before flip — the R3 xsec-expanded runner
template predates the selection-fair gate, so a portfolio runner cloned from it
inherits no gate block. **Guard recipe for the next portfolio slice:** a
portfolio runner must add the selection-fair gate block explicitly; do not
assume the R3 xsec template carries it.

## Close-out

**Done** (7 commits):
- `8f752ef` — born-red FIRST commit: this session card (`in-progress` hold).
- `a79de47` — the `xsec_drawdown` portfolio family + R7D grid
  (`_R7D_XSEC_DRAWDOWN_AXES`/variants/total) + pinning & causality tests
  (plan-before-outcome: the 6-variant grid pinned before any run).
- `c186f6c` — run the `xsec_drawdown` slice (6 configs, XSEC-14 portfolio).
- `4e63284` — results doc + `docs/current-state.md` (6 lanes graded,
  5,595 → 5,601).
- `9e9c1dc` — selection-fair gate run per lane (standing-rule-1 compliance;
  fixed==searched, gap=0 for the 1-config portfolio lanes).
- `9eed29a` — results doc gate blocks + `reason_class` from the gate.
- (this commit) — close-out flip: badge `in-progress` → `complete`,
  headline + previous-session-review + close-out slots resolved.

**Verify:**
- `python3 -m pytest -q` → 734 passed.
- `python3 bootstrap.py check --strict` → EXIT 0 at flip (the born-red
  in-progress hold clears when the badge flips to `complete`).
- Integrity: the branch diff touches only the R7D `xsec_drawdown`
  family / grid / tests + `scripts/run_r7d_*` + the
  `experiments/sweeps/r7d-xsec-drawdown` + `experiments/runs` +
  `index.jsonl` artifacts + `docs/research-round-7d-results.md` +
  `docs/current-state.md` + this session card. The holdout was never read,
  no fetch, `experiments/paper/**` untouched, `control/status.md` untouched,
  no triggers, no broker code, the `min_tstat` bar unchanged, promotion
  CLOSED / 0 promoted. NO manual merge — the auto-merge enabler lands PR #148
  on green.

**Next (guard recipe):** portfolio runners must run the selection-fair gate
explicitly — the R3 xsec-expanded runner template predates the gate, so a
runner cloned from it inherits no gate block and must add one (this cost
`9e9c1dc` here). The `render_round_results.py` generator idea from the R7
card would also generate the portfolio results docs, removing the hand-copied
gate-block-per-lane step. The drawdown effect did **not** survive
cross-sectionally (6/6 KILL, best informational t −0.74), closing that
question for the XSEC-14 daily surface — no return-vs-drawdown ranking-key
gradient remains to litigate on this basket.
