# 2026-07-18 — Round 8 run: execute the pre-registered hourly companion sweep

> **Status:** `complete` — Round 8 executed — hourly companion (R7 families on
> the 8-ticker hourly cache): 3 KEEP-dev / 13 KILL / 0 KILL-SIG of 16 lanes, 0
> promoted, best informational t 1.079 (MSFT drawdown_reversion) vs the
> unchanged 2.638 bar; gate 8 PASS / 8 FAIL; high_proximity nulled harder on
> hourly (0 KEEP-dev vs 2 daily) as the registered null predicted, all 3
> drawdown_reversion dev-KEEPs rest on the single ~8.5-month 2024 regime (R5-B
> caveat); 1/16 lanes (GLD hourly) UNGRADEABLE_NAN — the mandated
> infrastructure alarm, never strategy evidence; runtime 4.8 s vs 900 s cap;
> program 5,601 → 5,793. Results: docs/research-round-8-results.md.

📊 Model: opus-4.8 · round-8-run lane (overnight worker, owner GO
2026-07-18T13:47Z) · start 2026-07-18T16:08:33Z

## Why this session exists

Owner GO 2026-07-18T13:47Z authorizes executing the pre-registered Round-8
plan (`docs/research-round-8-plan.md`, badge `binding`, merged as PR #149,
PLAN-ONLY until now). Round 8 adds NO new strategy code: it is a pure
TIMEFRAME COMPANION that re-sweeps the two NEWEST single-instrument R7
families — `drawdown_reversion` (R7-A) and `high_proximity` (R7-B) — VERBATIM
on the committed 8-ticker hourly cache, isolating the single variable of bar
frequency. This session adds the two 12-variant hourly grids + pinning tests
to `trading_lab.sweeps` in the first code commit (plan-before-outcome), clones
the R6-C hourly runner so the selection-fair gate runs on EVERY lane, runs the
16-lane / 192-config slice, and writes `docs/research-round-8-results.md`.
POST-HOLDOUT, DEV-ONLY: the holdout is SPENT and promotion is CLOSED — a KEEP
anywhere is a dev-candidate only, never a finding. The `min_tstat` bar
(~2.638 at K=12) is UNCHANGED. Registered prior: hourly is the program's most
reliable KILL amplifier (15/16 KILL in R6-C), so the expected outcome is more
honest nulls, plausibly KILL-SIG lanes as hourly churn is cost-eaten.

## Work log

- branch `claude/round-8-run` cut from origin/main HEAD (incl. #149, the R8
  pre-registration). Born-red FIRST commit: this card.

💡 **Session idea:** the R6-C hourly runner and this R8 clone differ only in
six literals (SLICE, families tuple, per-family variant helper, instruments
tuple, the grid-drift assert dict, and the results-doc path) yet duplicate the
entire ~360-line run_lane→gate→ledger→index pipeline. A single parametrized
`run_hourly_companion(slice_name, families, variants_fn, instruments, expected_grid)`
in a shared `scripts/_hourly_runner.py` would let each timeframe-companion
round register only those six literals and inherit the tested gate-every-lane
pipeline verbatim — anchor: the byte-identical `run_lane` body across
run_r6_volume_hourly_sweep.py and run_r8_hourly_sweep.py; test target: a golden
test that the shared runner reproduces an existing slice's lane JSONs exactly.
(Distinct from the round-7c-run card's grid-expansion-dedup idea — that dedupes
`sweeps.py` axes expansion; this dedupes the `scripts/run_*` runner body.)

## Previous-session review

⟲ PR #149 (`r8-plan`, merged) — the BINDING Round-8 pre-registration
(`docs/research-round-8-plan.md`, badge `binding`): an hourly companion
re-sweeping the R7 families `drawdown_reversion` (R7-A) and `high_proximity`
(R7-B) VERBATIM on the committed 8-ticker hourly cache, (12+12)×8 = 192
configs, program 5,601 → 5,793, with a registered null (hourly amplifies
KILLs) and the single-2024-regime / 5-split caveat declared up front. **Did
this run honor the pre-registration? Yes, on every axis.** The RUN swept
exactly 192 configs = (12+12)×8 with the two grids VERBATIM from the committed
R7-A/R7-B axes (the pinning tests assert grid identity), and
`R8_HOURLY_INSTRUMENTS` resolved to `R6_VOLUME_HOURLY_INSTRUMENTS` exactly as
the plan pinned. Critically the selection-fair gate ran on EVERY lane via the
cloned R6 hourly runner (`scripts/run_r8_hourly_sweep.py`), so R8 does NOT
repeat the R7-D gap (#148's gate-less xsec runner that needed the gate restored
in a follow-up) — the plan's #148-derived guard recipe (clone the
gate-carrying runner) held, and the gate graded a real non-degenerate
fixed-vs-searched gap on 12-variant hourly lanes (8 PASS / 8 FAIL), the first
slice to exercise that path off the gap=0 special case. The registered null
**held cleanly for `high_proximity`** (0 KEEP-dev on hourly, harder-nulled than
the 2 daily KEEPs), and **split-but-substantively-held for
`drawdown_reversion`** — its 3 dev-KEEPs are exactly the weakest possible
finding because they rest entirely on the plan's own declared single-2024
~8.5-month regime, not a null-breaker. **One honest nit:** the plan did NOT
pre-declare that GLD hourly could come back UNGRADEABLE_NAN (a flat-strategy
degeneracy on a thin/degenerate instrument) — R8 caught it as the mandated
infrastructure alarm, but future hourly/thin-instrument plans should
pre-declare possible UNGRADEABLE_NAN degeneracy as a guard recipe. No
correctness defect found in #149.

## Close-out

**Done** (5 commits):
- `c8d4e65` — born-red FIRST commit: this session card (`in-progress` hold).
- `a4dc661` — hourly grids (R7 families) + pinning tests: `_R8_HOURLY_*_AXES`
  (= R7-A/R7-B axes VERBATIM) + `R8_HOURLY_INSTRUMENTS`
  (= `R6_VOLUME_HOURLY_INSTRUMENTS`) + variant fns + `r8_total_configs()` = 192.
  NO new strategy modules — reused the committed R7-A/R7-B families.
- `cdadabf` — run the 16-lane / 192-config hourly companion sweep via the
  R6-hourly-cloned runner `scripts/run_r8_hourly_sweep.py` (gate on every lane).
- `198c4c9` — `docs/research-round-8-results.md` + `docs/current-state.md`
  (program 5,601 → 5,793, 16 hourly lanes graded).
- (this commit) — close-out flip: badge `in-progress` → `complete`, headline +
  previous-session-review + close-out slots resolved.

**Verify:**
- `python3 -m pytest -q` → 745 passed.
- `python3 bootstrap.py check --strict` → EXIT 0 at flip (the born-red
  in-progress hold clears when the badge flips to `complete`).
- Integrity: the branch diff touches only the R8 block in
  `trading_lab/sweeps.py` + `tests/test_sweeps.py` +
  `scripts/run_r8_hourly_sweep.py` + `experiments/sweeps/r8-hourly` +
  `experiments/runs` + `experiments/runs/index.jsonl` +
  `docs/research-round-8-results.md` + `docs/current-state.md` + this session
  card. NO new strategy modules (reused R7-A/R7-B); the holdout was never read,
  no fetch, `experiments/paper/**` untouched, `control/status.md` untouched, no
  triggers, no broker code, the `min_tstat` bar unchanged, promotion CLOSED /
  0 promoted. NO manual merge — the auto-merge enabler lands PR #150 on green.

**Next (guard recipe):** the `render_round_results.py` generator (R7 card 💡)
would mechanize these results docs from the lane JSONs + gate ledger, removing
the hand-written results-doc step. Future hourly/thin-instrument plans should
pre-declare possible `UNGRADEABLE_NAN` degeneracy (the GLD hourly nit above) so
a flat-strategy lane reads as expected-and-handled rather than a surprise
alarm. With R8 landed, the R7 families are now tested daily (R7), conjoined
(R7-C), cross-sectionally (R7-D), and hourly (R8) — all null — closing the
R7-family question set.
