# 2026-07-13 — Round 6 run: execute the three pre-registered slices

> **Status:** `complete` — ORDER 014 item 2 delivered: all three
> pre-registered Round-6 slices executed top-down against the binding
> plan (PR #116 `c0e6459`), every lane graded under the selection-fair
> standing gate ([D-0002]) with the gate block in every lane JSON.
> Headline **3 KEEP-dev / 47 KILL / 8 KILL-SIG of 58 lanes, 0 promoted**
> — best t anywhere 0.60 vs the unchanged 2.638 bar; the overnight-gap
> family is the program's sharpest null ever (0/12, 7 KILL-SIG). Gate 17
> PASS / 41 FAIL, 0 KEEPs demoted; `selection_gap > 0` on only 2/58.
> Runtime caps nowhere near hit (25.6/12.0/7.9 s vs 1200/900/1200 s
> caps) — no CAP-HIT, nothing truncated, **no slice remains unrun**.
> Results: `docs/research-round-6-results.md` (badge `reference`).
> Burden: 696 registered configs consumed exactly as pre-declared,
> program cumulative 4359 → 5055. Both round-6 claims cleared in this
> close-out commit.

📊 Model: fable-5 · round-6-run lane (coordinator-dispatched night worker, ORDER 014 item 2) · start 2026-07-13T22:57Z

💡 **Session idea (deduped against recent cards — distinct from the
round-6-plan card's machine-readable burden ledger, the
selection-fair-gate card's `reason_class` taxonomy, the round-5 card's
standing-gate idea, and the kill-sig-review card's verdict-grammar
unification):** the three R6 runners are ~330-line near-copies of each
other (as the r3 runners were), and the [D-0002] gate call now lives in
each copy as duplicated prose — the exact copy-paste surface where a
future runner forgets the gate and violates the standing rule. Promote
the now-stable lane pipeline (full-period rows → walk-forward → ledger →
Round-2 rule → `classify_verdict` → `run_selection_gate`/`apply_gate` →
lane JSON with the gate block) into `trading_lab` as a single library
entry point (e.g. `trading_lab.lanerunner.run_registered_lane(...)`), so
per-slice scripts shrink to grid/instrument/cap declarations and gate
compliance becomes STRUCTURAL instead of copied. Anchors: the three
identical `run_lane()` bodies in `scripts/run_r6_*_sweep.py`; test
target: a contract test asserting the shared pipeline always writes a
`selection_gate` block before any KEEP verdict.

## Why this session exists

ORDER 014 item 2 (`control/inbox.md` @ `c9297a7`): run the Round-6 slices
once the plan is committed (plan-before-outcome). The plan merged as PR
#116 (`c0e6459`, `docs/research-round-6-plan.md`, badge `binding`); the
live claim is `control/claims/2026-07-13-round-6-run.md` (landed via PR
#114). This session executes exactly the three pre-registered slices —
R6-A `r6-volume` (360 configs, cap 20 min), R6-B `r6-gap` (144 configs,
cap 15 min), R6-C `r6-volume-hourly` (192 configs, cap 20 min) — top-down,
under the selection-fair gate ([D-0002]) on every lane, and writes
`docs/research-round-6-results.md`.

## Work log

- branch `claude/round-6-run` cut from origin/main HEAD `c0e6459`
  (PR #116 squash). Born-red FIRST commit: this card (`ca1427f`).
- 2026-07-13T23:02Z — R6-A `r6-volume` (`scripts/run_r6_volume_sweep.py`,
  commit `5e8baac`): 30 lanes / 360 configs in **25.6 s** (cap 1200 s) —
  2 KEEP-dev (BTC-USD `obv_trend` t 0.60, META `obv_trend` t 0.18, both
  gate PASS) / 27 KILL / 1 KILL-SIG (MSFT `mfi_reversion` t −2.89); gate
  9 PASS / 21 FAIL, 0 demotions; fidelity guard 30/30 within 1e-8.
  Control-arm answer: confirmed-arm mean full-period Sharpe 0.471 vs
  pure-OBV 0.479 — the volume channel adds nothing detectable.
- 2026-07-13T23:05Z — R6-B `r6-gap` (`scripts/run_r6_gap_sweep.py`,
  commit `6aad0a6`): 12 lanes / 144 configs in **12.0 s** (cap 900 s) —
  0 KEEP / 5 KILL / 7 KILL-SIG (t down to −4.01 on AAPL); gate 0/12
  PASS. The sharpest registered null in six rounds; both mirror theses
  (fade/follow) fail everywhere.
- 2026-07-13T23:07Z — R6-C `r6-volume-hourly`
  (`scripts/run_r6_volume_hourly_sweep.py`, commit `a511a6a`): 16 lanes /
  192 configs in **7.9 s** (cap 1200 s) — 1 KEEP-dev (AMZN
  `mfi_reversion` t 0.29, 5 splits, carries the registered R5-B
  single-window-luck caveat) / 15 KILL; gate 8/8; zero INSUFFICIENT-DATA,
  zero UNGRADEABLE (the plan's ≤ 10 zero-volume-bars fact never bit).
- 2026-07-13T23:12Z — results doc `docs/research-round-6-results.md`
  (badge `reference`, round-5 grammar, full per-lane fixed-config-row
  tables for all 58 lanes) + `docs/current-state.md` round-6 entry
  updated (commit `00dd834`). Heartbeat `control/status.md` item-2 lines
  (commit `f615a41`, second-to-last). Close-out: this commit (card flips
  `complete`; claim `control/claims/2026-07-13-round-6-run.md` deleted —
  the round-6-plan claim was already cleared by PR #116's own close-out).
- Ledger convention honored: 58 new ledger rows (top full-period variant
  per lane, `variants_tried=12`, Round-3 convention);
  `experiments/index.jsonl` rebuilt after each slice; gate replays
  report-only inside the lane JSONs (R4-B precedent).

## Previous-session review

⟲ PR #116 (round-6 pre-registered plan, squash `c0e6459`, card
`.sessions/2026-07-13-round-6-plan.md`) — this session executed against
its discipline, which is the strongest possible test of a plan PR, and
it held up end-to-end: every grid count the runner asserts
(`{obv_trend: 12, mfi_reversion: 12}` × 15 = 360; `{overnight_gap: 12}`
× 12 = 144; hourly identity × 8 = 192) matched `trading_lab.sweeps` at
HEAD exactly; the instrument tuples were verbatim as pinned; all three
new strategy families satisfied the prefix-invariance/causality contract
in anger — 58/58 searched-arm gate replays reproduced their recorded
stitched OOS Sharpe within 1e-8, which would have failed loudly on any
look-ahead; and its guard recipe for this session (call the gate before
any verdict, honor the caps with CAP-HIT protocol, delete the run claim,
inherit the zero-volume-bars fact) was complete and correct. One
reading nit, not a defect: the plan's "~5,065 hourly bars" describes the
RAW committed caches (raw AAPL hourly: 5,067 rows through 2026-07); the
dev rail the slices actually grade on truncates at `HOLDOUT_START` to
~2,476 bars / 5 splits — the plan itself states the ≈ 5-split geometry
two sentences later, so the numbers coexist honestly, but a future
reader should know both figures are true of different rails. No defect
found in its work.

## Close-out

**Done:** on branch `claude/round-6-run`, seven commits: `ca1427f`
(born-red card) → `5e8baac` (R6-A: runner + 30 lane JSONs + summary +
30 ledger rows) → `6aad0a6` (R6-B: runner + 12 lane JSONs + summary +
12 ledger rows) → `a511a6a` (R6-C: runner + 16 lane JSONs + summary +
16 ledger rows) → `00dd834` (results doc + current-state link) →
`f615a41` (heartbeat) → this close-out (card flipped `complete`, claim
deleted).

**Verify:** `python3 -m pytest -q` → **646 passed** (no test touched —
this session added runners and artifacts, not library code).
`python3 bootstrap.py check --strict` → green with this card complete
(pre-flip it held exactly one red: the designed born-red hold).
Integrity at close: diff vs main touches only `scripts/run_r6_*.py` (3
new runners), `experiments/sweeps/r6-*/` (58 lane JSONs + 3 summaries),
`experiments/runs/` (+58) + `experiments/index.jsonl` (rebuilt),
`docs/research-round-6-results.md` (new), `docs/current-state.md` (round-6
entry), `control/status.md` (heartbeat), `.sessions/`, and the claim
deletion. Every pre-existing `experiments/**` file byte-untouched;
`data/p5holdout/` never read, `unlock_holdout` never passed; no data
fetched (committed caches only); `control/inbox.md`/`outbox.md`
byte-untouched; `scripts/grade_paper.py` and `experiments/paper/**`
untouched; no triggers created or modified; no broker/order/exchange
code; R5-C BTC OOS not scheduled; MTF-Bollinger untouched (FROZEN);
min_tstat bar never lowered; promotion CLOSED — 0 promoted; NO merge
action by this session on its own PR — the auto-merge enabler is the
landing path.

**Next (guard recipe):** (1) the three KEEP-devs enter the dev-candidate
surface at the BOTTOM of the stability ladder — none has seen the
R5-A/B/C battery; a future stability round should target them before
anyone cites them, and the R6-C KEEP (AMZN `mfi_reversion` hourly)
carries the explicit R5-B 5-split caveat — anchor:
`docs/research-round-6-results.md` § R6-C, precedent
`scripts/run_r5b_split_stability.py`. (2) If a round 7 exists, its
runners should not be a fourth copy of `run_lane()` — see this card's 💡
(structural gate compliance via a shared pipeline).

Session end: badge flipped `complete` in this final content commit
before push.
