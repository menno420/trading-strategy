# 2026-07-18 — Round 8 pre-registration plan (hourly companion for the R7 families)

> **Status:** `in-progress`

📊 Model: opus-4.8 · r8-plan lane (overnight worker, owner GO 2026-07-18T13:47Z / menu #03) · start 2026-07-18T15:58Z

## Why this session exists

Menu #03 (overnight menu): pre-register the FIRST hourly companion round for
the two newest single-instrument families — Round 8, an HOURLY companion that
re-sweeps the COMMITTED R7 families (`drawdown_reversion` R7-A and
`high_proximity` R7-B) VERBATIM on the committed 8-ticker hourly cache
(AAPL, AMZN, GLD, GOOGL, META, MSFT, NVDA, SLV), plan-before-outcome. Round 7
explicitly DECLINED every hourly slice (round-6 re-confirmed hourly as "a KILL
amplifier at baseline costs (15/16)"; R5-B demoted the program's only prior
hourly KEEP for single-window luck). This round answers the declared-but-
deferred hourly question for exactly those two families under the same
plan-before-outcome discipline, with a null prior fixed in advance and the
single-2024-regime caveat registered up front (not offered post-hoc). NO new
strategy code — the round reuses the committed R7-A/R7-B families and their
IDENTICAL 12-variant grids; it adds only hourly sweep grids + a runner (cloned
from the R6-C hourly runner) + pinning tests. This session writes the BINDING
pre-registration (`docs/research-round-8-plan.md`, badge `binding`) only. PLAN
ONLY — this session runs NOTHING, commits no grid to `sweeps.py`, and touches
no strategy / backtest code; executing R8 is a future session's separately
claimed slice. POST-HOLDOUT, DEV-ONLY: the holdout is SPENT and promotion is
CLOSED — a KEEP anywhere is a dev-candidate only, never a finding. The
`min_tstat` bar (≈2.638 at K=12) is UNCHANGED and never lowered.
Build-then-review: implement, open PR READY, leave the card `in-progress` for
the owner's correctness check before flip.

## Work log

- branch `claude/research-round-8-plan` cut from origin/main HEAD 656f390
  (#148, R7-D; program cumulative 5,601). Born-red FIRST commit: this card.

💡 **Session idea:** stand up a small `docs/timeframe-companion-index.md` — a
one-row-per-(family × timeframe) table of every family the program has swept on
a NON-daily bar, with the bar frequency, the calendar horizon each window
number actually spans at that frequency (e.g. a 252-bar window is ~1 trading
year daily but ~39 sessions hourly), the split count / bars-per-lane the dev
rail affords at that frequency, and the standing verdict. The whole hazard of a
hourly companion is that the SAME window number means a DIFFERENT calendar
horizon, and the thin hourly dev rail affords far fewer splits — a companion
index makes the horizon-shrink and the evidence-thinness legible per row and
stops "is this the same family or a new one" being re-litigated after outcomes.
Anchor: the `Declare explicitly` horizon line in this R8 plan, the
`R3_*_HOURLY_*` / `R6_VOLUME_HOURLY_*` symbols in `sweeps.py`, and the R6-C
hourly results block (1/16 KEEP-dev, 5 splits). (Distinct from the
cross-sectional ranking-key index idea, which rolls up ranking KEYS within the
portfolio family class — this rolls up BAR FREQUENCIES across the single-name
family classes.)

## Previous-session review

[[fill:]]

## Close-out

[[fill:]]
