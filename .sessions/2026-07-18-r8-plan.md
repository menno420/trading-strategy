# 2026-07-18 — Round 8 pre-registration plan (hourly companion for the R7 families)

> **Status:** `complete` — Round 8 pre-registered (PLAN ONLY) —
> docs/research-round-8-plan.md (binding): hourly companion re-sweeping the R7
> families drawdown_reversion + high_proximity on the 8-ticker hourly cache
> (R8_HOURLY_INSTRUMENTS = R6_VOLUME_HOURLY_INSTRUMENTS), (12+12)×8 = 192
> configs, program 5,601 → 5,793; registered null (hourly amplifies KILLs) +
> single-2024-regime/5-split caveat; running is a future slice; dev-only,
> promotion CLOSED.

📊 Model: opus-4.8 · r8-plan lane (overnight worker, owner GO 2026-07-18T13:47Z / menu #03) · start 2026-07-18T15:58Z

## Why this session exists

Menu #03 (overnight menu): pre-register the FIRST hourly companion round for
the two newest single-instrument families — Round 8, an HOURLY companion that
re-sweeps the COMMITTED R7 families (`drawdown_reversion` R7-A and
`high_proximity` R7-B) VERBATIM on the committed 8-ticker hourly cache
(AAPL, AMZN, GLD, GOOGL, META, MSFT, NVDA, SLV), plan-before-outcome. Round 7
explicitly DECLINED every hourly slice (round-6 re-confirmed hourly as "a KILL
amplifier at baseline costs (15/16)"; R5-B demoted the program's only prior
hourly KEEP for single-window luck). This round answers the declared-but-
deferred hourly question for exactly those two families under the same
plan-before-outcome discipline, with a null prior fixed in advance and the
single-2024-regime caveat registered up front (not offered post-hoc). NO new
strategy code — the round reuses the committed R7-A/R7-B families and their
IDENTICAL 12-variant grids; it adds only hourly sweep grids + a runner (cloned
from the R6-C hourly runner) + pinning tests. This session writes the BINDING
pre-registration (`docs/research-round-8-plan.md`, badge `binding`) only. PLAN
ONLY — this session runs NOTHING, commits no grid to `sweeps.py`, and touches
no strategy / backtest code; executing R8 is a future session's separately
claimed slice. POST-HOLDOUT, DEV-ONLY: the holdout is SPENT and promotion is
CLOSED — a KEEP anywhere is a dev-candidate only, never a finding. The
`min_tstat` bar (≈2.638 at K=12) is UNCHANGED and never lowered.
Build-then-review: implement, open PR READY, leave the card `in-progress` for
the owner's correctness check before flip.

## Work log

- branch `claude/research-round-8-plan` cut from origin/main HEAD 656f390
  (#148, R7-D; program cumulative 5,601). Born-red FIRST commit: this card.

💡 **Session idea:** stand up a small `docs/timeframe-companion-index.md` — a
one-row-per-(family × timeframe) table of every family the program has swept on
a NON-daily bar, with the bar frequency, the calendar horizon each window
number actually spans at that frequency (e.g. a 252-bar window is ~1 trading
year daily but ~39 sessions hourly), the split count / bars-per-lane the dev
rail affords at that frequency, and the standing verdict. The whole hazard of a
hourly companion is that the SAME window number means a DIFFERENT calendar
horizon, and the thin hourly dev rail affords far fewer splits — a companion
index makes the horizon-shrink and the evidence-thinness legible per row and
stops "is this the same family or a new one" being re-litigated after outcomes.
Anchor: the `Declare explicitly` horizon line in this R8 plan, the
`R3_*_HOURLY_*` / `R6_VOLUME_HOURLY_*` symbols in `sweeps.py`, and the R6-C
hourly results block (1/16 KEEP-dev, 5 splits). (Distinct from the
cross-sectional ranking-key index idea, which rolls up ranking KEYS within the
portfolio family class — this rolls up BAR FREQUENCIES across the single-name
family classes.)

## Previous-session review

⟲ PR #148 (`round-7d-run`, merged) — executed the pre-registered Round-7D
`xsec_drawdown` cross-sectional slice: ranked the XSEC-14 basket by trailing
drawdown depth (`close/rolling(L).max()-1`) over the 6-variant grid
(`L`{63,126,252} × `k`{2,3}, rebalance frozen 21). **Outcome: 6/6 KILL, 0
promoted, best informational t −0.74 vs the K=6 bar ≈2.39** — drawdown STATE
carried no cross-sectional edge where the return-ranked neighbors and the
single-name R7-A drawdown lanes had already nulled; all 6 lanes underperform
the 1.193 equal-weight B&H but none significantly (0 KILL-SIG), so
uninformative rather than value-destroying. The registered null held on every
axis and the family + grid + pinning/causality tests landed in the first code
commit before any run outcome existed. The one substantive fix was compliance,
not correctness: the R3-cloned xsec runner shipped WITHOUT the selection-fair
gate and standing-rule-1 (run the gate on every lane) had to be restored in a
follow-up commit (`9e9c1dc`) before flip — the R3 xsec-expanded template
predates the gate, so any runner cloned from it inherits no gate block. **How
this directly informs THIS plan:** #148's guard recipe — a portfolio/sweep
runner must run the selection-fair gate explicitly, and a gate-less template is
a trap — is exactly why this R8 plan pins the RUN slice to CLONE the R6 HOURLY
runner (`scripts/run_r6_volume_hourly_sweep.py`), which already runs the gate on
every lane, rather than an older gate-less template. Cloning the
gate-carrying runner turns #148's after-the-fact fix into a before-the-fact
default and spends no commit re-adding the gate. **One honest nit:** #148's
`xsec_drawdown` lanes are single-config portfolio lanes, so its selection-fair
gate is degenerate there (fixed==searched, gap=0) and exercises the gate's
grading path only trivially; R8's hourly single-name lanes are genuine
multi-config sweeps (12 variants each), so R8 will be the first slice to
exercise the cloned runner's gate on a NON-degenerate search — worth watching
that the gate grades a real fixed-vs-searched gap on the hourly rail, not the
gap=0 special case #148 saw. No correctness defect found in #148.

## Close-out

**Done** (3 commits):
- `f1fcd63` — born-red FIRST commit: this session card (`in-progress` hold).
- `2bbab61` — the BINDING pre-registration `docs/research-round-8-plan.md`
  (badge `binding`) + `docs/current-state.md` pointer (program 5,601 → 5,793
  on the future RUN). PLAN ONLY — no grid committed to `sweeps.py`, no
  strategy/backtest code, no runner.
- (this commit) — close-out flip: badge `in-progress` → `complete`, headline +
  previous-session-review + close-out slots resolved.

**Verify:**
- `python3 -m pytest -q` → 734 passed.
- `python3 bootstrap.py check --strict` → EXIT 0 at flip (the born-red
  in-progress hold clears when the badge flips to `complete`).
- Integrity: the branch diff touches only `docs/research-round-8-plan.md` +
  the `docs/current-state.md` pointer + this session card. PLAN ONLY — no
  code / grid / run / verdict / holdout / paper / control-status / trigger
  changes; the holdout was never read, no fetch, `experiments/paper/**`
  untouched, `control/status.md` untouched, the `min_tstat` bar unchanged,
  promotion CLOSED / 0 promoted. NO manual merge — the auto-merge enabler
  lands PR #149 on green.

**Next (the R8 RUN slice, a future separately-claimed session):** land the
sweeps + runner + results in code commits, plan-before-outcome. First code
commit adds `_R8_HOURLY_DRAWDOWN_AXES` + `_R8_HOURLY_HIGHPROX_AXES` (= the R7-A
/ R7-B axes VERBATIM) + `R8_HOURLY_INSTRUMENTS` (= `R6_VOLUME_HOURLY_INSTRUMENTS`,
the 8-ticker hourly cache) + the variant fns + `r8_total_configs()` = (12+12)×8
= 192 + pinning tests — NO new strategy modules, reuse the committed R7-A
`drawdown_reversion` / R7-B `high_proximity` families. Then CLONE
`scripts/run_r6_volume_hourly_sweep.py` (gate on every lane, timeframe hourly —
per #148's guard recipe, the gate-carrying runner) and land
`docs/research-round-8-results.md`.
