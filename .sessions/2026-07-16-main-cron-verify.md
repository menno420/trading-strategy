# 2026-07-16 — Durable main-cron verification: host-owned workflow + kit-file trigger revert

> **Status:** `complete`

- **📊 Model:** fable-5 · medium · implement/verify

⚑ Scope (claimed, `control/claims/2026-07-16-main-cron-verify.md`, branch
`claude/main-cron-verify`): make yesterday's main-CI cron durable. PR #133
(squash `fb741e1`) appended `schedule: cron '23 */6 * * *'` +
`workflow_dispatch:` directly to `.github/workflows/tests.yml` and
`.github/workflows/substrate-gate.yml` — but substrate-gate.yml's own header
says `bootstrap.py upgrade` REGENERATES that file in place ("hand edits are
OVERWRITTEN") and instructs host customizations to live in a SEPARATE
workflow file. A future kit bump would silently strip the triggers. Rails:
research-only, no broker/exchange-write/live-API config, no trigger
create/delete/fire, no workflow_dispatch fired, `control/inbox.md`
byte-untouched, holdout SPENT and untouched.

## What / why

1. NEW host-owned `.github/workflows/main-cron-verify.yml`: carries the
   `schedule: '23 */6 * * *'` + `workflow_dispatch:` triggers;
   `permissions: contents: read`, no secrets; checkout pins `ref: main` so
   scheduled/dispatched runs always verify main; mirrors the same
   verification the PR checks run (setup-python 3.11 → `pip install -r
   requirements.txt` → `python3 -m pytest -q` → `python3 bootstrap.py check
   --strict` via the gate's no-card sentinel lane). Header documents that
   the file is host-owned and NOT regenerated by `bootstrap.py upgrade` —
   exactly the placement substrate-gate.yml's header prescribes.
2. REVERT the three trigger lines #133 appended to each of tests.yml and
   substrate-gate.yml (`schedule:` / `- cron: '23 */6 * * *'` /
   `workflow_dispatch:`), restoring both `on:` blocks byte-identical to
   kit-template shape so future `bootstrap.py upgrade` diffs stay clean.
   Verified against `git show fb741e1` — nothing beyond those lines
   touched.

## Files touched

- `.github/workflows/main-cron-verify.yml` — NEW (host-owned cron verify)
- `.github/workflows/tests.yml` — revert #133 trigger lines only
- `.github/workflows/substrate-gate.yml` — revert #133 trigger lines only
- `control/claims/2026-07-16-main-cron-verify.md` — claim (this session)
- `control/status.md` — heartbeat overwrite (this seat is its one writer)
- this card

## Evidence SHAs

- Base: `fb741e1` (= origin/main at boot, hard-sync clean)
- #133 diff reverted from: `fb741e1` (`.github/workflows/{tests,substrate-gate}.yml`, +3 lines each)
- Local verify at base: recorded in the work log below

## Close-out

**Previous-session review:** the 2026-07-16 main-ci-triggers session
(PR #133, squash `fb741e1`) correctly diagnosed the green-by-proxy gap and
shipped working triggers same-session — but placed them inside the two
kit-owned workflow files whose own header warns hand edits are OVERWRITTEN
on `bootstrap.py upgrade`; durable placement (this session's fix) was the
header-prescribed separate host-owned file. Card + claim hygiene on #133
was otherwise clean.

💡 **Session idea (deduped against prior cards):** the substrate check
already prints a NOTE that `scripts/preflight.py` is absent (config
`preflight_scripts`). Planting a minimal preflight script that runs the
same pytest + strict-check pair as `main-cron-verify.yml` would converge
the local ritual, the PR gate, and the new cron workflow on ONE check
list — three surfaces, one definition. (Anchors: `bootstrap.py` check
NOTE output; `.github/workflows/main-cron-verify.yml` steps.)

## Close-out note

**Owner approved live (Y, 2026-07-16).** The completion flip and the
landing decision were owner-gated (platform merge-review classifier
ruling); the owner ruled live in-session, replying "Y" to the explicit
"flip the card + land #134?" ask on 2026-07-16. This commit flips the
card to `complete`; substrate-gate re-runs green on the new head and the
already-armed auto-merge (squash) lands PR #134. The earlier
substrate-gate red was the designed born-red HOLD working as intended.
